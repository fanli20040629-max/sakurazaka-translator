# 当前状态与验收记录

日期：2026-09-15。交付版本：core_modules v0.2。

交付目录：/Users/fanli0629/Downloads/审核0914/0914审核结果/core_modules。目标目录已重新运行自测，39 项检查通过；与制作目录逐文件比较一致。原长期稳定版开发总方案与 source_snapshot 未覆盖。

## 当前事实

| 项目 | 代码/文档 | 自动验证 | 主工程/设备 |
|---|---|---|---|
| 11 个纯 Java 核心类 | 已实现，有中文注释 | 39 项自测检查通过 | 未接入另一台电脑主工程 |
| 消息排序与已确认正文整理 | 已实现 | 排序、过滤、重复和未知边界检查通过 | 真实消息边界检测待实现/真机确认 |
| 符号校验 | 已修复草稿只检查 contains 的缺陷 | 重复、顺序、Emoji 组合、换行和显式保护片段检查通过 | OCR 是否识别对符号未验证 |
| 翻译请求/结果/风格/接口 | 已实现契约 | 请求身份、风格版本、空返回和符号检查通过 | 无真实 API 实现、无付费调用 |
| 跨电脑指南与模块地图 | 已编写 | 路径与本包文件对照检查 | 尚无 GitHub 仓库 URL/目标完整 SHA |
| macOS/Linux 测试脚本 | 已实现 | macOS 默认路径、显式源码路径、bash 语法检查通过 | Linux 尚未实跑 |
| Windows 测试脚本 | 已实现 | 静态检查 | 当前无 PowerShell 环境，未执行 |
| 原审核快照逻辑测试 | 未修改原源码 | ProbeLogicSelfTest PASS | 不等于 Android 生命周期/资源已实机正确 |
| Android APK / 悬浮层 / OCR / Vision | 本次未接入 | 未构建 APK | 未真机验证 |

## 本地实测环境和命令

系统：macOS；可用工具为 PyCharm 随附 JBR 25.0.3。系统默认 java/javac 未配置；测试通过临时 JAVA_HOME 指向现有 JDK，不安装、不修改全局环境。

```bash
JAVA_HOME='/Applications/PyCharm.app/Contents/jbr/Contents/Home' bash core_modules/scripts/verify-core.sh
JAVA_HOME='/Applications/PyCharm.app/Contents/jbr/Contents/Home' bash core_modules/scripts/verify-core.sh core_modules/src/com/fanli/sakurazakatranslator/domain
bash -n core_modules/scripts/verify-core.sh
```

两次核心测试均输出 CoreModulesSelfTest: PASS (39 checks)，退出码 0。编译参数 --release 17 -encoding UTF-8 -Xlint:all -Werror，无编译警告。注意：这是 JDK 25 上按 Java 17 编译约束运行，尚未在实际 JDK 17 或 Android ART 上验证。

原快照的 ProbeLogic.java、ProbeModels.java、TextAssembly.java、CaptureCoordinator.java 和 tools/ProbeLogicSelfTest.java 使用同一 JDK/语言约束编译到临时目录后执行，输出 ProbeLogicSelfTest PASS。没有改写快照来使测试通过。

回归证据：新增“两个爱心被删成一个必须校验失败”检查，在旧 SymbolProtector 上出现 AssertionError；修复后通过。新增模型/请求接口测试先因缺少这些接口编译失败，实现后全部通过。

本包不依赖第三方测试库；39 表示独立断言/异常检查数量，不是 39 个端到端设备场景。自动测试不证明日语到中文的语义准确、美观或长期稳定。

## 已知限制与下一步

1. 没有 Git 主仓库，不能生成适用于未知主工程的可靠 git patch，也未创建/推送任何 GitHub 提交。
2. 未读取另一台电脑的未提交代码、依赖锁定和 SDK。先由 Luna 执行接入指南阶段 A。
3. 核心与早期草稿的接口有更新；若另一台电脑已复制旧草稿，需同时迁移调用和测试，不能混用旧 Builder、旧 Provider 内嵌 Result。
4. 不提供 MessageBoundaryDetector、StyleProfileRepository、BlogStyleAnalyzer、DeepSeek 实现和 Overlay 空壳。这些在真实接入阶段才按实际接口实现。
5. 没有新增 TranslationCoordinator，以免与 CaptureCoordinator 产生第二个生命周期状态源。
6. 新模型未携带完整诊断 provenance/warnings；迁移期保留旧快照证据，按 pageToken/messageId/fragmentId 关联，不静默丢弃；禁止把敏感证据上传公共仓库。
7. 本包的 record 数据模型生成的 toString 可能含私人内容，禁止直接记生产日志。
8. 真机测试要在节点边界、气泡定位、图片授权三个关键阶段短检查，最后再集中回归。最后一天第一次上手机风险较大。

## Luna 每阶段填写的记录格式

把记录保存到主工程 docs/迁移状态.md，每阶段保留历史：

- 阶段及实际目标。
- 起始完整 Git SHA、完成提交 SHA（未提交则明确写未提交）。
- 修改文件及每个文件的修改原因。
- 当前唯一生产调用路径；已退役旧路径及检索证据。
- 执行命令、退出码、测试结果。
- APK 是否构建/安装；具体版本与证据位置。
- 真机是否验证；设备型号、系统、操作步骤、预期/实际结果。
- 仍未通过的条目、需要用户执行的具体动作、下一阶段。

不同状态不能互相替代。例如：核心逻辑通过 + APK 未构建 + 真机未测，是合法且诚实的阶段交付；不能写成“长期稳定版已完成”。
