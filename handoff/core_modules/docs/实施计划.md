# 核心模块与跨电脑接入计划

目标：把已授权的纯 Java 模块草稿完善为可验证的增量接入包，交给另一台笔记本的主工程接入。

架构：现有 Android 项目是主工程；本目录是待接入源码，不是第二套 App。保留现有采集入口，以 CaptureCoordinator 为唯一总流程迁移起点。暂不生成另一套生产状态机、Android UI 或真实网络实现。

技术：Java 17 语言级别、JDK 自测，无新增第三方依赖。没有 Git 主仓库，不创建虚假的提交记录。

## 已确认边界

- 用户已确认按模块完善并附中文注释，不重复请求设计确认。
- 依据审核快照（README 标注 1302cd6，尚未在 Git 验证），不能假定远端主工程完全相同。
- 优先级：准确性约等于美观性，略高于 token 消耗，略高于响应速度；原开发总方案将前两项写为严格大于，接入时应记录此差异，以用户表述为准。
- 核心只整理已确认属于聊天消息的片段；真实节点分组与 OCR 置信度须真机验证。
- 符号校验保留重复次数与顺序；无法识别的图形/颜文字不能声称已经保护。

## 执行任务

- [x] 1. 添加可跨平台运行的测试脚本，先重现重复符号丢失仍通过的问题。
- [x] 2. 修复 SymbolProtector；覆盖波浪号、爱心、Emoji 组合、换行、显式颜文字片段及非法输入。
- [x] 3. 完善消息模型、已确认消息整理器、风格版本和翻译请求/结果契约；先用测试明确过滤和排序规则。
- [x] 4. 写单一阅读入口、模块地图、基线/依赖核查步骤、分批迁移及回退操作、设备测试节点和 Luna 首轮任务。
- [x] 5. 运行全部核心自测及原快照逻辑自测；记录实测环境、未验证项。文件摘要在最终交付生成。
- [x] 6. 交付包已保存到用户指定的 0914审核结果/core_modules，并在目标位置重新运行 39 项检查通过；source_snapshot 未修改。

## 本次验收

所有核心测试通过；仅 Java 标准库；源代码有职责与限制说明；接入文档明确主工程尚未修改、不能覆盖已有同名类、不能把自测通过等同于 APK 或真机通过。跨平台脚本在能执行的本机验证，另一平台记录为未实测。
