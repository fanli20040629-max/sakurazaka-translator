# 开发状态

更新时间：2026-09-12

## P0

- [x] 工具链路径已检查：Android Studio、SDK 36、Build Tools 36.0.0、Platform-Tools、ADB、Git、Java。
- [x] 单模块 Android 工程骨架已创建（当前用 Java 降低首次构建依赖）。
- [x] Gradle 9.1.0 本地构建链可用，首次 `assembleDebug` 已完成。
- [x] P0/P0.5 APK 已生成，尚未安装到真实设备。

## P0.5

- [x] 无障碍服务声明和设置入口。
- [x] 最小悬浮探针按钮。
- [x] 节点文本读取代码。
- [x] 指定窗口截图和内存图像转换代码。
- [x] bundled 日文 OCR 依赖接入并进入 APK；静态 native pipeline 检查通过。
- [x] 已有修复代码的逻辑自测、Wrapper 构建和 lint（0 error）通过。
- [ ] 第 28.7 节 A1～A5 行为收尾及针对性检查；尚未实施。
- [ ] 合成页面自测（代码样本已补齐，手机运行未验证）。
- [ ] 目标 App 四类页面真机测试。

## 证据边界

“源码已写入”不等于“构建通过”；“APK 已构建”不等于“已安装”；“截图成功”不等于“正文取字通过”。所有未运行项保持未验证。

## 下一步

当前为可构建探针，P0.5 未通过。静态检查通过不代表生命周期及页面身份行为全部正确；最新执行细则见开发方案第 28.7 节。

1. 完成 A1～A5 收尾，运行针对性测试、lint 和构建，生成新 APK。
2. 手机连接并安装后，完成 B 合成自测和 C 四类目标页面；分别记录授权、截图、OCR 和正文验收证据。
3. 正文门槛通过后执行 D 翻译开发，最后 E 扩展验收。

## 2026-09-12 当前交付检查点

- [x] A1～A5 生产代码收尾完成，并已按最终 diff 再审查。
- [x] 逻辑测试实际执行：`ProbeLogicSelfTest PASS`，输出覆盖取消重点击、旧回调、销毁/迟到截图模型、OCR 成功/失败/启动异常、资源单次释放、页面身份、锁屏和卡片滚动。
- [x] `:app:lintDebug` 成功：0 error、1 个 Gradle 9.1.0 版本提示。
- [x] `:app:assembleDebug` 成功；最终 APK 已完成签名、版本、包名、权限和日文模型静态检查。
- [ ] 安装成功：ADB 无设备，未执行。
- [ ] 截图成功：未执行。
- [ ] OCR 正文通过：未执行。
- [ ] 真机验收/P0.5：未执行，不能宣称通过。

最新 APK：`D:\Projects\sakurazaka-translator\app\build\outputs\apk\debug\app-debug.apk`；SHA-256 `1B8C9B66EFE45189B05E382DFE54E2A7839D3C250B39E81062034B4E1A676799`；大小 `53,683,519` 字节。该包进一步过滤探针自身 `TYPE_WINDOW_STATE_CHANGED`，并保留普通内容变化、悬浮窗窗口事件过滤及移除卡片前解除 Bitmap 引用，待手机复测。

### 合成页真机复测结果

- 卡片稳定性：通过；可滑动、可关闭。
- 截图：通过；显示当前合成页，尺寸为 `1080×2340`。
- 图片日文 OCR：通过；已识别 `画像内だけの日本語です。` 和 `OCR で二行目も確認します。`。
- 混排正文：未完全通过；日文模型对中文控件文本有误识别。P0.5 尚不能整体宣称通过。
