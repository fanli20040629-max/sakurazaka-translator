# 开发状态

## 2026-09-16 当前源码状态

- 最新入口：[本轮优化验收与交接](本轮优化验收与交接.md)。源码版本标记为 0.3.1-probe-review / 4。
- 已落实协调器改名/阶段约束、节点/OCR 抽离、统一选择状态、原文排序修复、请求预览、空白/重复 ID 检查、跨电脑逻辑测试入口。
- 七组桌面自测通过；尚未运行本轮 Android 编译、lint、Windows PowerShell 脚本或真机测试。
- OCR 行不等于聊天消息；作者/时间尚未从 OCR 自动过滤；无翻译 API。
- 查验结果和下一步门槛：[当前代码审核与改进方案](当前代码审核与改进方案.md)。下方旧 APK/hash/测试状态仅用于追溯。

以下历史记录更新时间：2026-09-14

## 2026-09-14 历史状态：M2/M3 第一批已安装

- 当前入口：[PROBE_TEXT_OPTIMIZATION_HANDOFF.md](PROBE_TEXT_OPTIMIZATION_HANDOFF.md) 版本 2.0；主方案 28.11；按 M0～M5 开发，T01～T25 分层验证。
- 2026-09-14 用户提供的新版目标页截图显示：指定窗口截图成功（`1080×2340`）；节点路径可读取长消息段落并保留换行；另一页日文 OCR 输出 13 个块，一次样本耗时 `836ms`。图片没有显示 APK 哈希，因此安装包身份仍按“与当前界面一致、未独立确认”记录。
- 真机结果同时确认了下一阶段问题：来源标签和坐标占据正文界面；作者、时间、导航、正文和语音时长混排；OCR 会把部分符号识别成多余字符。需要完成 M2 正文分类与 M3 OCR 候选对照。P0.5 未通过，不进入翻译。
- 已据此完成第一批修正：节点结果按正文、作者/时间、其他文字、控件/媒体分区；同节点相同 text/description 合并但保留双来源；坐标不再进入默认阅读区；OCR 行成为默认不选的复选候选，勾选后单独汇总，不自动覆盖节点正文。
- 本次已审查代码、查阅 Android/ML Kit 官方资料，并完成 M1 第一批代码：ProbeModels、TextAssembly、CaptureCoordinator；服务已接入结构节点采集、OCR 完成监听、节点异常终止、用户关闭失效和 overlay 窗口登记。
- 扩展后的 verify-probe-logic.ps1 与 ProbeLogicSelfTest PASS；`:app:compileDebugJavaWithJavac --offline`、`:app:lintDebug`、`:app:assembleDebug` PASS。APK 静态检查 PASS：版本 `0.2.0-probe-text`/2，大小 `53,365,230` 字节，SHA-256 `A91A4E66C7A1140CBD165054C7C8D4710AE9F41CB8FBCB8B84EE84F11C4ED5FA`，v2 签名，未含 INTERNET/ACCESS_NETWORK_STATE，含日文 OCR 模型。ADB 当前无设备。
- D01～D04 的第一批修复已落地，但内容变化事件、完整资源终止矩阵和真实 OCR Task 仍需继续测试；旧 PASS 不等于生产生命周期全覆盖。
- `ProbeLogicSelfTest`、Java 编译、lint、assembleDebug 和 APK 静态检查通过。待测 APK：`0.3.0-probe-select`/3，大小 `53,383,422` 字节，SHA-256 `443F2341C476E4180C7E39CCA336B4E6F84C0C99C12C2C79E3E3308F456D9D63`；v2 签名，无网络权限，包含日文 OCR 模型。ADB 覆盖安装成功，设备端版本一致，无障碍服务仍启用，主界面已启动。下一步是真机操作目标页，复测新分区和候选交互；M2～M5 和 P0.5 仍未完成。
- 下方为历史记录，其中“未安装”“尚未实施”和测试覆盖声明不是当前结论；以最新入口中的证据边界为准。

## 历史状态

## P0

- [x] 工具链路径已检查：Android Studio、SDK 36、Build Tools 36.0.0、Platform-Tools、ADB、Git、Java。
- [x] 单模块 Android 工程骨架已创建（当前用 Java 降低首次构建依赖）。
- [x] Gradle 9.1.0 本地构建链可用，首次 `assembleDebug` 已完成。
- [x] P0/P0.5 APK 已生成，尚未安装到真实设备。

## P0.5

- [x] 无障碍服务声明和设置入口。
- [x] 最小悬浮探针按钮。
- [x] 节点文本读取代码。
- [x] 指定窗口截图和内存图像转换代码。
- [x] bundled 日文 OCR 依赖接入并进入 APK；静态 native pipeline 检查通过。
- [x] 已有修复代码的逻辑自测、Wrapper 构建和 lint（0 error）通过。
- [ ] 第 28.7 节 A1～A5 行为收尾及针对性检查；尚未实施。
- [ ] 合成页面自测（代码样本已补齐，手机运行未验证）。
- [ ] 目标 App 四类页面真机测试。

## 证据边界

“源码已写入”不等于“构建通过”；“APK 已构建”不等于“已安装”；“截图成功”不等于“正文取字通过”。所有未运行项保持未验证。

## 下一步

当前为可构建探针，P0.5 未通过。静态检查通过不代表生命周期及页面身份行为全部正确；最新执行细则见开发方案第 28.7 节。

1. 完成 A1～A5 收尾，运行针对性测试、lint 和构建，生成新 APK。
2. 手机连接并安装后，完成 B 合成自测和 C 四类目标页面；分别记录授权、截图、OCR 和正文验收证据。
3. 正文门槛通过后执行 D 翻译开发，最后 E 扩展验收。

## 2026-09-12 当前交付检查点

- [x] A1～A5 生产代码收尾完成，并已按最终 diff 再审查。
- [x] 逻辑测试实际执行：`ProbeLogicSelfTest PASS`，输出覆盖取消重点击、旧回调、销毁/迟到截图模型、OCR 成功/失败/启动异常、资源单次释放、页面身份、锁屏和卡片滚动。
- [x] `:app:lintDebug` 成功：0 error、1 个 Gradle 9.1.0 版本提示。
- [x] `:app:assembleDebug` 成功；最终 APK 已完成签名、版本、包名、权限和日文模型静态检查。
- [ ] 安装成功：ADB 无设备，未执行。
- [ ] 截图成功：未执行。
- [ ] OCR 正文通过：未执行。
- [ ] 真机验收/P0.5：未执行，不能宣称通过。

最新 APK：`D:\Projects\sakurazaka-translator\app\build\outputs\apk\debug\app-debug.apk`；SHA-256 `1B8C9B66EFE45189B05E382DFE54E2A7839D3C250B39E81062034B4E1A676799`；大小 `53,683,519` 字节。该包进一步过滤探针自身 `TYPE_WINDOW_STATE_CHANGED`，并保留普通内容变化、悬浮窗窗口事件过滤及移除卡片前解除 Bitmap 引用，待手机复测。

### 合成页真机复测结果

- 卡片稳定性：通过；可滑动、可关闭。
- 截图：通过；显示当前合成页，尺寸为 `1080×2340`。
- 图片日文 OCR：通过；已识别 `画像内だけの日本語です。` 和 `OCR で二行目も確認します。`。
- 混排正文：未完全通过；日文模型对中文控件文本有误识别。P0.5 尚不能整体宣称通过。
