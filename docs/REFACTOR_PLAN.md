# 渐进式重构执行计划

## 最新增量：0.5.0 中文翻译试用版

用户已确认先开发后回机验证。本轮计划与验收以 [TRANSLATION_TRIAL_PLAN](TRANSLATION_TRIAL_PLAN.md) 和 [本轮交接](本轮优化验收与交接.md) 为准。下方“本轮仍是离线探针”只指 0.4 阶段；0.5 已增加显式文本联网翻译，Android 构建与真机仍未通过验收。

## 2026-09-17：原文保真与统一候选（已获用户确认）

基线：`6214f15`。目标是节点和 OCR 共用选择流程，保留 Unicode 原文、来源、风险和页面身份；本轮仍是离线探针。

设计：沿用唯一 CaptureCoordinator；OcrSelection 改为 CandidateSelection，不保留第二套选择实现。PageToken 从采集开始生成，传到候选和请求；关闭/切页清空。节点与 OCR 的坐标未验证时分源排序，不猜自动融合或消息边界。浮窗仅抽出展示职责，不复制采集逻辑。

- [x] 先添加能失败的保真/来源/页面测试，再实现统一候选：节点先显示、OCR 追加不重置选择、重复或迟到结果拒绝。
- [x] 完善确认项：完整边界及坐标类别、来源 ID、风险随请求传递；不将 OCR 错字猜成 Emoji。
- [x] 接入生产 UI：节点可选，辅助信息默认排除，预览/诊断折叠，版本可见；关闭、失效和截图失败保留正确语义（代码已写入，Android 运行待测）。
- [x] 运行同一清单的纯 Java 测试、差异/编码检查；七组通过，Java/XML 语法通过。本机缺少 Android SDK，已明确交接 Windows 构建验证。
- [x] 同步版本、产物检查脚本和当前交接入口，记录已执行检查及真机待测项。
- [ ] Windows 完成新版 Android 构建、lint、APK 验证；执行交接中的 C01～C13 真机矩阵。

验收：组合 Emoji/换行原样经过请求；同文不同位置不误删；节点选择不被 OCR 返回重置；旧 pageToken 无法更改新选择；请求保留边界/警告；无节点/OCR 自动覆盖。桌面测试不代表 Android 回调、窗口及真机通过。

以下为 2026-09-16 的历史执行记录。

更新时间：2026-09-16。实际验证范围见 [本轮优化验收与交接](本轮优化验收与交接.md)。

- [x] domain 模型与 TranslationRequestFactory 接入请求预览（仍是片段，消息分组未实现）。
- [x] CaptureCoordinator 接入生产并限制阶段流转；ResourceLease 保持单次释放策略。
- [x] NodeTreeReader 与 OcrProcessor 抽离；主服务保留页面有效性和资源所有权。
- [x] OcrSelection 统一候选/勾选状态；按坐标稳定排序；关闭清空；界面明确片段不是消息。
- [x] 删除未调用的旧节点拼接和无用导入；DomainSelfTest 移至 tools。
- [x] Mac/Windows 共用测试清单；Mac 执行七组 PASS；七项新增回归先复现失败再修复。
- [x] 修正构建中的机器专用路径与缺失调试签名文件处理，待 Gradle 验证。
- [ ] 在原电脑运行 PowerShell 自测、Android 编译、lint 和 assembleDebug，记录新 APK。
- [ ] 真机验证浮窗、取字、勾选、关闭、锁屏和迟到回调，不能用纯模型测试代替。
- [ ] 基于真机证据做消息类型/边界、局部 OCR、符号校验、网络和中文显示；具体门槛见审核报告。

当前不继续扩大浮窗重构；先用新包确认此次节点/OCR拆分保持行为，再抽离窗口解析和浮窗控制器。
