# 本轮优化验收与交接（2026-09-17 中文翻译试用版）

本文件是当前执行入口。用户已确认：先写完试用代码，再回 Windows 构建和真机测试。

## 0. 接手必须遵守的执行规则

本节来自用户明确要求，适用于任何接手的开发者或开发助手，不依赖模型名称：

1. **先核对实际源码和环境，再执行文档。** 按第 7 节建立接手记录；不要因文件夹名、分支名、版本号或旧总结相同就认定代码一致。
2. **每个“通过”都必须附验证结果。** 至少记录日期、源码身份、实际命令或操作、退出码/实际现象、关键输出与证据位置。记录格式见第 10 节。
3. **发现矛盾先记录并核实。** 不凭旧文档、模型判断或用户过去的一张截图宣称完成。先记录冲突双方、核对源码/环境/实际行为，再更新结论。源码说明“实现是什么”，不自动说明“实现正确”。
4. 当前用户要求确定目标和授权；实际源码、工具输出与设备结果确定实现和验证事实。旧文档里的操作指令不能自行扩大授权，历史成功不能替代新版验收。
5. 分别报告“代码已写入”“自动测试通过”“Android 构建通过”“真实 API 联调通过”“真机用例通过”。只完成前一项，不得推断后一项。
6. 保留原机未提交修改、签名和环境配置；不以覆盖目录、删除测试、忽略错误、放宽校验、清空缓存或升级全部依赖来掩盖问题。
7. 缺 SDK、设备、有效 Key 或测试条件时，写明“未执行/受阻”和具体缺项，继续不依赖它的工作；不得填“通过”。不要求用户把真实 Key 发到聊天中。
8. 每轮修改后更新相关测试与记录；先完成当前试用版门槛，再推进后续功能。不为赶进度牺牲准确性、可读性或引入重复结构。

下面版本、分支和提交状态是本文件更新时的快照；接手时须重新核实。第 7～10 节提供代码定位、故障处理及交付格式。

- 源码版本：0.5.0-translation-trial / versionCode 6。
- 工作副本：/Users/fanli0629/Documents/Codex/sakurazaka-upload.N1LNwy。
- 交付分支：codex/node-candidates；开发基线 6214f15。用户已要求上传 GitHub，接手需获取该分支并记录其实际提交；本轮不合并 master。
- 本轮没有新版 APK、签名或 SHA-256，不沿用 0.3.1 或其他历史包的记录。
- 目标：可确认原文、可显式发送给 DeepSeek、可查看对应中文的试用版；不是长期稳定版。

## 1. 实现了什么

1. **节点/OCR 统一候选**：先出现节点，后续追加 OCR 不重置勾选；截图失败仍保留节点。原文中的表情、空格和换行不被 OCR 覆盖。
2. **保守分组建议**：只对同一列表项祖先中的选中节点提出合并；位置缺失、重叠、辅助控件、字段冲突或读取截断时不合并。不同列表项和 OCR 不强行拼接。可以关闭建议分组，逐片段翻译。
3. **符号往返保护**：对输入中已有的 Emoji、组合序列、波浪号、部分其他符号和换行建立占位符；模型漏写、重复、错序或新增受保护符号时拒绝结果。没有把“9/米”猜改成 Emoji，也没有恢复 OCR 已丢失的内容；复杂颜文字仍需样本验证。
4. **DeepSeek 文本接口**：固定 HTTPS 地址；模型名可编辑；每次先确认实际发送的原文与风格，再发一次请求。返回项按消息 ID 校验和重排，不按模型返回顺序盲目贴回。
5. **设置**：首页入口可输入 API Key、模型，以及两个可编辑的偶像名称/中文风格摘要；手动选择当前档案。Key 用 Android Keystore 加密后存储，不回显、不进日志、不进仓库；空 Key 输入保留原值，删除有独立按钮。
6. **中文卡片**：在当前结果卡片中按编号显示原文和中文。不是自动定位到目标 App 原气泡旁边的显示层。
7. **取消与隔离**：改选择、改分组、关卡片、切页或锁屏后取消旧翻译显示。仍在结束的物理请求占用不被伪装清空，不启动第二个并行翻译。45 秒总截止、10 秒连接和 25 秒读取超时；不自动重试收费请求。
8. **设置页隔离**：密钥设置页禁截图，并从助手取字范围排除；这必须在手机验证。备份/设备迁移规则显式排除共享偏好，避免迁移无法解密的凭据。

当前不会自动辨认所有作者/时间，也不会自动知道哪位偶像正在聊天；所有候选默认不选。Blog 的自动导入/分析、云端图片识别和跟随消息的中文气泡尚未实现。

## 2. 实际验证情况

- 九组桌面纯 Java 测试通过，包括上一轮候选测试 41 项、新翻译/分组逻辑测试 22 项、翻译单任务与迟到回调测试。
- JSON 协议和 HTTPS 边界共九项 JUnit 测试通过，使用本地内存模拟，不使用真实 Key、不发送真实请求。覆盖占位符往返、错误 ID、截断响应、字段类型、重定向拒绝、限流、取消及响应体积限制。
- 生产 Java 和 XML 语法检查；新增设置、界面及核心模块另用 Android 16 类库做类型检查。此部分检查排除依赖 ML Kit 的 Service/OcrProcessor，并使用临时 R 占位类；不代表 Gradle、Android 资源编译、lint、打包或运行通过。
- 本机无完整 Android SDK。接口真实调用、Android 构建、Keystore、权限/签名产物检查和全部真机用例仍未执行。
- 本地验证用的下载依赖和临时文件均在被 Git 忽略的 .verification 中，不属于工程交付物。运行依赖仍为原 ML Kit；仅增加 JUnit 与 org.json 测试依赖。
- DeepSeek 官方文档站本轮 DNS 访问失败。实现按兼容 Chat Completions 文本协议，默认模型 deepseek-chat 可修改；回机时须核实账号当前可用模型、JSON 输出支持、限额与价格。

最终测试数与检查记录见 [优化执行记录](优化执行记录.md)。不把本地模拟通过写成真实 API 或手机成功。

## 3. 同步到另一台电脑

传递完整工程差异，包括新增与删除文件、app/src/test、tools、scripts、docs 和构建配置，不只复制几个 Java 文件。

先保留原机未提交修改和 Git 检查点，再合并本轮内容。保留原机 SDK 设置、local.properties、debug.keystore 和构建缓存；不覆盖整个原工程，不将 handoff/core_modules 历史草案覆盖进 app。本轮交付目标是 codex/node-candidates 分支，单独 git pull master 得不到这些修改。

阅读顺序：本文 → [实施计划](TRANSLATION_TRIAL_PLAN.md) → [审核与后续规划](当前代码审核与改进方案.md) → [长期目标](长期稳定版开发总方案.md) → [Windows 编码说明](WINDOWS_ENCODING.md)。

GitHub 获取方式：先按第 7 节检查并保留原机修改，再执行只下载远程引用的操作：

~~~powershell
git fetch origin
git log -1 --oneline origin/codex/node-candidates
~~~

确认原机工作区干净，且尚无同名本地分支时，执行：

~~~powershell
git switch --track origin/codex/node-candidates
git status --short
git rev-parse HEAD
~~~

若已有同名本地分支，先比较本地与远程差异，再切换并仅在可快进时更新；存在分叉或未提交修改先合并核对，不 force/reset。安装前仍须完成本文构建与产物验证，不因下载完成就宣称真机可用。

## 4. Windows 构建命令

在同步后的工程根目录运行，JAVA_HOME 和 SDK 环境变量使用原机真实有效路径：

~~~powershell
& "$env:JAVA_HOME\bin\java.exe" -version
& .\scripts\verify-pre-device.ps1 -ExpectedVersionCode 6 -ExpectedVersionName '0.5.0-translation-trial'
~~~

未配置环境变量时，可给脚本传原机实际路径的 -JavaHome 和 -SdkRoot。保留 Java 17 源码目标、SDK 36、minSdk 34 和现有 ML Kit 版本。

总入口现在执行：九组逻辑测试 → Android Java 编译 → testDebugUnitTest（协议/HTTP 测试）→ lint → assembleDebug → APK 版本/签名/权限/OCR 模型检查。任一步失败应停止安装并修复，不跳过测试。

**权限变更是有意的**：文本翻译需要 INTERNET；ACCESS_NETWORK_STATE 仍不需要，脚本继续要求其不存在。旧版“最终包必须无 INTERNET”的要求只适用于离线探针，不能原样套用到本版本。Manifest 禁用明文网络，HTTP 代码不跟随重定向。

成功后记录新 APK 大小和 SHA-256，再核对设备端 versionCode 6、versionName 和浮窗标题。签名不一致先找回原调试签名，不自动卸载或清除数据。

## 5. 真机测试顺序与通过标准（全部待测）

先用合成数据，再用用户自选聊天。设置中只输入自己的 Key，不发到聊天或 GitHub。

| 编号 | 操作 | 通过标准 |
| --- | --- | --- |
| C01 身份/范围 | 安装新版，合成页取字，切换到未允许 App | 版本正确；浮窗只在允许页出现；旧正文不残留 |
| C02 节点优先 | OCR 未完成时选节点，等 OCR 结束 | 选择保留、没有重复卡片；截图失败时节点仍能选 |
| C03 原文保真 | 比较波浪号、爱心、组合 Emoji、换行、同文不同位置 | 原文透传一致；重复位置各自保留；OCR 错字不被猜改 |
| C04 分组建议 | 选合成页两条列表项的所有行，再关闭分组；目标页重复此操作 | 同条可建议合并，不跨条；关分组后独立；无依据不合并；记录目标页不分组或错分组 |
| C05 辅助信息 | 作者/时间/按钮默认不选；补选真正的“20:00”正文 | 不丢失可恢复正文；补选保持人工确认语义 |
| C06 卡片交互 | 展开/折叠、拖动、滚动、关闭 | 不因自身事件误关闭；关键按钮可达，文字可读 |
| C07 旧页失效 | 截图/OCR/翻译中切页、锁屏、关卡片、改变选择 | 旧结果不上屏；重开无旧确认/译文；重复点击无并行翻译 |
| C08 设置隔离 | 开合成模式后进入设置，输入测试 Key，再返回 | 设置页无取字按钮/旧卡片；不能采集 Key；返回后取字可用 |
| C09 Key 生命周期 | 保存真实 Key、重启助手、留空保存风格、删除 Key | 能解密使用；留空不删除；删除后不可再发送；失败不退回明文存储 |
| C10 原文确认 | 只点准备不发送；切换分组或选择；重新确认 | 不提前请求；展示实际条目、服务商、模型与风格；改选择使旧确认失效 |
| C11 实际中文 | 少量日文分别使用两份风格翻译 | 返回中文与编号对应，含义和语气合理；符号/换行保留；风格选择明确 |
| C12 网络错误 | 错 Key、断网、慢网、受限额度 | 提示清楚、原文仍在；无自动重试；恢复后显式重发可用 |
| C13 连续使用 | 约 20 次取字/选择/翻译/关闭，穿插旋转、字体调整和服务重启 | 无崩溃、旧页串入或任务叠加；记录耗时及内存趋势 |

优先阻塞项：C01、C02、C06～C10。卡片误关、旧结果重现或设置页被采集时，先修复再测真实内容。不能通过忽略所有同包/内容事件来掩盖问题。

已发送的请求可能由服务商继续处理或计费，取消只能停止本机等待/展示并尝试断开，不能撤回已上传文字。测试记录不要附 Key、真实订阅正文或未脱敏截图。

## 6. 后续目标

1. 修复本轮真机和真实 API 联调发现的问题；确认两位偶像页面是否有可用列表项结构。
2. 用实际窗口/节点边界验证坐标映射、完整消息归属和局部 OCR，补足分组规则；不按距离盲目合并。
3. 对节点也缺失的符号提供局部图像核对路线，核实视觉服务能力后单独实现上传。
4. 在消息边界可靠后做原消息附近的中文显示，再做自动 Blog 分析、风格版本管理和长期稳定性回归。

## 7. 接手的第一步：核对源码与环境

### 7.1 确认拿到的是完整修改

在原电脑的实际工程根目录执行，只读检查，不先 pull、覆盖或重置：

~~~powershell
Get-Location
git rev-parse --show-toplevel
git branch --show-current
git rev-parse HEAD
git status --short
git diff --stat
git diff --cached --stat
git ls-files --others --exclude-standard
Get-Content -Encoding UTF8 .\app\build.gradle.kts
Get-Content -Encoding UTF8 .\tools\logic-sources.txt
Get-Content -Encoding UTF8 .\tools\logic-test-classes.txt
~~~

核对并记录：

- 包名应为 com.fanli.sakurazakatranslator，本次起始版本预期 0.5.0-translation-trial / 6。后续修复升版时同步脚本和记录，不为了符合旧文档降回版本 6。
- 第 8 节所有生产和测试文件实际存在；原来的 OcrSelection/OcrSelectionFormatter 已由 CandidateSelection/CandidateTextFormatter 替换，不能留下旧类并另建同职责实现。
- 新翻译设置、确认发送、协议、Provider、Runner 和对应测试都在，不能仅凭版本号证明接收完整。
- 开发基线为 6214f151e6003b9b16d97b20caee88dad95a44fb；它不包含本轮实现。上传后应以 origin/codex/node-candidates 实际提交为交付身份；不要把基线当成新版本。同一 HEAD 若有未提交修改也不代表两台电脑源码相同。
- 先查看原机已修改及未跟踪文件，分别确认哪些是本轮移入、哪些是原机工作。重叠改动逐处合并，保留双方有价值的修改；无法判明用户意图的冲突再请用户决定。
- 没有 .git 的导出副本不能伪报提交号。以“导出副本”登记，和原机仓库比较新增/修改/删除文件；保留对照副本直到构建和测试通过。

优先以明确的 Git 提交和干净工作区作为可复现源码身份。若保留未提交内容，记录基线 HEAD、修改/未跟踪清单及实际传输包 SHA-256；对含源码的包先检查排除 Key、签名、聊天截图、本地配置和缓存，再传递。不要声称一份 diff --stat 就证明逐字一致。

### 7.2 确认原机工具链

~~~powershell
$PSVersionTable.PSVersion
Get-Command git, java -ErrorAction SilentlyContinue | Select-Object Name, Source
$env:JAVA_HOME
$env:ANDROID_SDK_ROOT
$env:ANDROID_HOME
Get-Content -Encoding UTF8 .\gradle\wrapper\gradle-wrapper.properties
Get-Content -Encoding UTF8 .\build.gradle.kts
~~~

只检查所需变量，不打印所有环境变量或整个用户 Gradle 配置，以免带出凭据。检查原机 local.properties 中的 sdk.dir 时只记录 SDK 路径，不复制其全部内容。

核实 JAVA_HOME 指向的 java.exe 存在、SDK 含 platforms/android-36、build-tools/36.0.0、platform-tools，以及 Wrapper 所需缓存/下载条件。源码 Java 17 目标不等于 Gradle 运行 JDK 必须恰好是 17；以锁定的 Gradle/AGP 兼容要求和原机已验证 JBR 为准，不盲目替换运行 JDK。

检查后按第 4 节运行总入口。若要单独核对 Wrapper 使用的 JDK：

~~~powershell
& .\gradlew.bat --version
~~~

此命令可能下载缺失的 Wrapper 分发包；下载失败记为环境问题，不修改源码伪装修复。使用当前进程或脚本参数设置路径，避免无必要修改全局环境。

### 7.3 接手阶段应先输出什么

先在 [优化执行记录](优化执行记录.md) 追加一条“Windows 接手核验”，包含实际目录、分支/提交/脏状态、版本、关键新增文件完整性、运行 JDK、SDK/Build Tools、当前差异与缺项。没有阻塞就接着构建，不必为了这个例行记录反复要求用户确认。

## 8. 代码定位表：出现问题先查哪里

路径均相对工程根目录，可点击查阅。先检查调用链和复现条件，再修改最小相关范围。

| 现象或职责 | 优先查看的源码 | 关联验证 |
| --- | --- | --- |
| 目标范围、浮窗误关、切页后旧内容重现 | [主服务](../app/src/main/java/com/fanli/sakurazakatranslator/capture/TranslatorAccessibilityService.java)、[页面/窗口逻辑](../app/src/main/java/com/fanli/sakurazakatranslator/capture/ProbeLogic.java)、[页面身份](../app/src/main/java/com/fanli/sakurazakatranslator/domain/PageToken.java) | ProbeLogicSelfTest；C01/C06/C07/C08；桌面模型不证明系统事件正确 |
| 节点漏字、截断、辅助信息混入 | [节点读取](../app/src/main/java/com/fanli/sakurazakatranslator/capture/NodeTreeReader.java)、[文本整理](../app/src/main/java/com/fanli/sakurazakatranslator/capture/TextAssembly.java)、[节点模型](../app/src/main/java/com/fanli/sakurazakatranslator/capture/ProbeModels.java) | ProbeLogicSelfTest、ReviewRegressionSelfTest；C03/C05 |
| 勾选丢失、顺序错误、请求原文不一致 | [候选状态](../app/src/main/java/com/fanli/sakurazakatranslator/capture/CandidateSelection.java)、[原文汇总](../app/src/main/java/com/fanli/sakurazakatranslator/capture/CandidateTextFormatter.java)、[确认项工厂](../app/src/main/java/com/fanli/sakurazakatranslator/capture/ConfirmedMessageFactory.java) | CandidateSelectionSelfTest、CandidateTextFormatterSelfTest、ConfirmedMessageFactorySelfTest；C02/C03/C10 |
| 分组错误或没有自动合并 | [分组建议](../app/src/main/java/com/fanli/sakurazakatranslator/capture/MessageGrouper.java)、主服务 prepareTranslation | TranslationTrialSelfTest；C04。先确认目标是否提供 collectionItem，没依据不合并是预期回退 |
| OCR 本身错字或缺表情 | [OCR 适配](../app/src/main/java/com/fanli/sakurazakatranslator/capture/OcrProcessor.java)、原始节点/OCR 对照 | C03；先区分识别错误和后处理错误，不能在 SymbolProtector 里用替换规则猜回表情 |
| 译文表情/换行变化、消息对应错误 | [符号保护](../app/src/main/java/com/fanli/sakurazakatranslator/translation/SymbolProtector.java)、[协议](../app/src/main/java/com/fanli/sakurazakatranslator/translation/TranslationProtocol.java)、[结果校验](../app/src/main/java/com/fanli/sakurazakatranslator/domain/TranslationValidator.java)、[结果模型](../app/src/main/java/com/fanli/sakurazakatranslator/domain/TranslationResult.java) | TranslationTrialSelfTest、TranslationProtocolTest；C11 |
| 认证、模型、余额、网络或 HTTP 失败 | [DeepSeek 实现](../app/src/main/java/com/fanli/sakurazakatranslator/translation/DeepSeekProvider.java)、TranslationProtocol | DeepSeekProviderTest、TranslationProtocolTest；C11/C12；核实官方能力，不输出原始响应/Authorization |
| 取消、超时、重复请求、旧译文 | [翻译任务管理](../app/src/main/java/com/fanli/sakurazakatranslator/translation/TranslationRunner.java)、主服务 sendTranslation/removePreview | TranslationRunnerSelfTest；C07/C12/C13；取消不能立即伪造底层已结束 |
| Key 保存/删除/解密、风格切换 | [翻译设置存储](../app/src/main/java/com/fanli/sakurazakatranslator/translation/TranslationSettings.java)、[设置页面](../app/src/main/java/com/fanli/sakurazakatranslator/TranslationSettingsActivity.java)、[备份规则](../app/src/main/res/xml/data_extraction_rules.xml) | C08/C09/C11；Keystore 必须手机验证，不退回明文 |
| 确认内容不一致、中文看不见、按钮/滚动问题 | [候选面板](../app/src/main/java/com/fanli/sakurazakatranslator/capture/CandidatePanel.java)、[翻译面板](../app/src/main/java/com/fanli/sakurazakatranslator/capture/TranslationPanel.java)、[合成页面](../app/src/main/java/com/fanli/sakurazakatranslator/MainActivity.java) | C02/C06/C10/C11；不要在 View 内另建网络请求和状态协调器 |
| 图片释放、重复采集或 OCR 结束后卡死 | 主服务 CaptureJob/completeJob/removePreview、[采集协调器](../app/src/main/java/com/fanli/sakurazakatranslator/capture/CaptureCoordinator.java)、ProbeLogic.ResourceLease | ProbeLogicSelfTest、ReviewRegressionSelfTest；C02/C07/C13 |
| 构建、权限、版本、签名不符 | [模块构建](../app/build.gradle.kts)、[Manifest](../app/src/main/AndroidManifest.xml)、[构建总入口](../scripts/verify-pre-device.ps1)、[产物检查](../scripts/verify-android-artifact.ps1) | 第 4 节全链路；不能通过删除校验让旧 APK 混过关 |

纯 Java 自测源和运行类的唯一清单是 [logic-sources.txt](../tools/logic-sources.txt) 与 [logic-test-classes.txt](../tools/logic-test-classes.txt)。新增自测必须加入清单。Gradle 协议/HTTP 测试位于 [TranslationProtocolTest](../app/src/test/java/com/fanli/sakurazakatranslator/translation/TranslationProtocolTest.java) 和 [DeepSeekProviderTest](../app/src/test/java/com/fanli/sakurazakatranslator/translation/DeepSeekProviderTest.java)；源码存在不代表已执行。

## 9. 故障处理与矛盾核实顺序

### 9.1 统一处理步骤

1. 记录最先失败的阶段、精确命令/操作、源码和版本、关键错误类别。后续连带错误不要误当多个独立根因。
2. 用最小合成输入复现，按下面分流判断是环境、配置、代码还是目标页面适配问题。
3. 修改前记录预期与实际差异；纯逻辑/协议问题添加能复现失败的测试。Android 生命周期问题保留设备复现步骤，不能用另造的模型替代设备证据。
4. 做范围最小的修复，运行相关测试。通过后运行第 4 节总入口；任何产物相关代码/配置修改后重新生成和核对 APK。
5. 重新执行受影响的手机用例，并记录前后结果。C07 变化需同时复测 C02/C06/C13；设置/权限变化需复测 C01/C08/C09/C10；协议/符号变化需复测 C10/C11/C12。
6. 全部达到本阶段标准才写“该阶段通过”。有未测项就列明，不使用“全部完成”“长期稳定”等总括结论。

### 9.2 分流处理

| 首个失败点 | 先核实 | 处理边界与复验 |
| --- | --- | --- |
| Java/SDK/Wrapper 找不到、依赖下载失败 | 实际路径、运行 JDK、锁定版本、缓存和网络/TLS | 修正本机路径或按官方来源补依赖；不清空缓存、关闭 TLS 或升级全部依赖；重跑环境检查与总入口 |
| Java 编译、资源编译、lint 失败 | 首个具体报错文件、真实依赖 API、源与目标版本 | 修正调用/资源/具体规则；不全局压制 lint；重跑编译、测试、lint、构建和产物检查 |
| 测试失败或数量突然减少 | 测试清单、运行入口、实际失败断言及测试报告 | 修复生产行为或经证据确认的测试假设；不删断言凑 PASS；记录测试增删原因 |
| 安装失败 | APK 签名、设备原版本、包名和空间/系统要求 | 签名冲突找回原签名；不自动卸载或清数据；新包重新检查身份 |
| API 报认证/余额/限流 | 手机保存的配置是否有效、账号状态、错误类别 | 先修配置，不改分组算法；限流/余额场景不反复发送付费请求；无法自然触发的错误保留“模拟通过/真机未测” |
| API 报请求格式/模型不支持 | 当日官方文档、实际账号模型、JSON 格式与参数 | 记录资料链接/日期，做小量合成日文请求；不能以猜测模型名批量试错；同步协议测试和实际结果 |
| 符号校验拒绝结果 | 输入已有符号、保护前后文本、模型返回的占位符 | 用合成数据复现，保留严格 ID/符号校验；不要删除检查只为展示译文；不日志输出订阅正文 |
| 卡片误关、旧内容重现 | 目标窗口与浮窗 ID、事件类型、页面代次、任务结束顺序 | 脱敏记录，不忽略全部同包/滚动/内容事件；修复后执行生命周期组合用例 |
| 目标页面没有分组、OCR 不准 | 节点父子/列表项/坐标证据、文字是否来自图片 | 没有结构时保留逐片段确认；需要新适配规则则用样本证据设计，不假造边界或 Emoji |
| 文档说已完成而实际代码/结果不符 | 文档日期、对应提交/脏状态、运行包身份 | 先填写下方矛盾记录；按实测修正状态，不用旧 PASS 覆盖本次失败 |

### 9.3 矛盾记录模板

追加在 [优化执行记录](优化执行记录.md)，不要改写掉原始历史证据：

~~~text
矛盾编号/日期：
文档说法及位置：
实际源码/环境/设备证据：
影响范围：构建 / 数据对应 / 隐私 / UI / 文档等
初步原因：已确认事实与待验证假设分开写
核实步骤与实际结果：
处理结论：修代码 / 修环境 / 修文档 / 等待必要信息
修改文件与验证证据：
仍未解决事项及下一步：
~~~

例如：旧文档要求无 INTERNET，而版本 6 已获用户确认接入文本 API，应记录阶段变更并按新版权限验收；这不是删除网络功能的理由。若两份现行说明冲突且无法从用户意图和实测证据消解，暂停依赖该选择的工作，明确询问用户，不自行扩大功能或改变隐私范围。

## 10. 验证证据与最终交付模板

### 10.1 每项结论的证据要求

| 结论 | 必须提供 | 不能作为替代 |
| --- | --- | --- |
| 桌面逻辑通过 | 执行命令、时间、源码身份、退出码 0、实际运行的类与 PASS 数 | 只有源码文件、此前另机的 PASS、模型认为没问题 |
| 协议/HTTP 单测通过 | testDebugUnitTest 结果、tests/failures/errors/skipped 数，报告位置 | 编译成功、任务 NO-SOURCE、模拟测试被说成真实 API |
| Android 构建通过 | Wrapper/JDK/SDK 版本、具体任务、退出码、当前 APK | 语法解析、部分类型检查或旧 APK |
| lint 通过 | lint 报告中的错误/警告统计及未修警告说明 | 静默忽略错误、加 baseline 后不解释 |
| 产物验证通过 | 包名/versionCode/versionName、SHA-256、签名与权限/OCR 模型检查结果 | 浮窗样式相似、文件名相同 |
| 真实 API 联调通过 | 时间、App/模型、合成输入类别、实际中文和校验结果、网络/错误状态；脱敏记录 | 内存 HTTP 模拟、仅有 HTTP 200、不检查返回内容 |
| 真机用例通过 | C 编号、设备型号/系统、APK 身份、步骤、预期/实际、脱敏证据位置或具体人工观察记录 | 单张无版本截图、另一版本记录、没复现就默认通过 |

Gradle 报告常见位置：app/build/test-results/testDebugUnitTest、app/build/reports/tests/testDebugUnitTest/index.html、app/build/reports/lint-results-debug.html（或 XML）。接手时检查文件实际存在、生成时间与本轮源码对应；报告目录存在不能证明报告来自本轮。

若 Gradle 显示 UP-TO-DATE/FROM-CACHE，应如实标记复用而非重新运行；接手首轮不能证明测试缓存对应当前源码时，使用当前 Wrapper 的 testDebugUnitTest --rerun-tasks 强制执行测试并留证。NO-SOURCE、SKIPPED、未执行不算通过；缓存命中不自动代表失败，但必须明确证据来源。

原始日志仅在受控本地保存，复查脱敏后再提取摘要进文档；不把带 Key、正文、完整 API 响应或设备序列号的日志提交仓库。

### 10.2 接手后统一填写的交付记录

将自动检查摘要追加到 [优化执行记录](优化执行记录.md)，手机结果追加到 [DEVICE_TESTS](DEVICE_TESTS.md)。不要另建多个互相竞争的“最终状态”文档。

~~~text
日期与执行者：
工程实际目录：
分支 / HEAD / 未提交改动清单：
源码来源与完整性核对：提交或传输包 SHA-256；新增/修改/删除已核对
本轮目标及变更文件：
运行 JDK / Wrapper / AGP / SDK / Build Tools：

验证表（每项各填一行）：
阶段或用例 | 命令/步骤 | 源码或 APK 身份 | 退出码/实际现象
| 通过/失败/部分通过/未执行/受阻 | 关键结果 | 证据位置 | 剩余问题

桌面自测：实际类数及结果
Gradle 单测：tests / failures / errors / skipped；是否实际重跑
lint：错误数 / 警告数 / 剩余警告
APK：路径 / 包名 / versionCode / versionName / 字节数 / SHA-256
签名及权限/OCR 模型检查：
安装：结果与设备端版本；未安装写未执行
真实 API：服务商/模型、合成输入类别、结果；未调用写未执行
C01～C13：逐项状态，不能只填“真机通过”
矛盾及处理：记录编号、证据、最终结论
未解决事项：影响、可复现步骤、下一步、是否阻塞当前阶段
同步：本轮实际提交号 / 是否推送 / 分支；未提交或未推送明确写出
回退：实际存在的旧提交或安全副本；签名/数据兼容限制
下一步：只列尚未达到门槛的具体任务
~~~

测试完成后同步本文件顶部的源码/提交/验证快照及 README、project_state、STATUS 的当前摘要；历史记录保留日期。修复改变版本、权限或依赖时，同时改构建文件、验证脚本默认值与操作文档，不能只改一处。

### 10.3 可直接交给接手助手的指令

~~~text
请在这台电脑的实际 sakurazaka-translator 工程中接手当前中文翻译试用版。
先完整阅读 docs/本轮优化验收与交接.md，严格执行第 0 节和第 7～10 节。
先核对实际源码、未提交改动、文件完整性和原机环境，再执行文档。
保留原机修改、签名和配置；不要用历史 core_modules 覆盖生产代码。
每个“通过”必须附本次命令/操作、源码或 APK 身份、实际输出和证据位置。
发现文档、源码、环境或设备结果矛盾时先记录并核实，
不凭旧文档、历史截图或模型判断宣称完成。
先运行构建总入口并修复失败，再准备新版 APK 和 C01～C13 手机测试；
没有设备或 Key 的项目标记未执行，继续其他可验证工作。
用 docs/优化执行记录.md 和 docs/DEVICE_TESTS.md 记录结果，
按交接模板报告实际完成、未测项和后续阻塞，不重复搭建已存在模块。
~~~
