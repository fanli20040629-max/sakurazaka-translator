# 真机测试记录

## 2026-09-22 版本 8 待测入口（本轮尚无手机结果）

源码已调整至 8 / `0.7.0-reading-card-trial`，本地尚未提交/推送。已加入阅读卡、正文推荐、长文补取和默认关闭的快捷翻译。测试顺序与逐项标准统一见 [翻译卡实施与验收](2026-09-22翻译卡实施与验收.md)：R01～R05、L01～L04、W01、Q01～Q02 **全部待真机执行**。本机纯逻辑/模拟协议/类型检查通过不转记为设备通过。

成员识别新增 I01～I06：设置页先填写两位成员实际显示的日文姓名和可选完整别名。真机需验证标题结构能否读取；标题缺失或同名冲突时应停止快捷发送并允许手动确认。正文中的其他成员姓名不应改变当前风格；可靠换人必须清除旧草稿和阅读卡。

今晚先核对版本和构建、保留签名覆盖安装，再免 API 演示、真实正文/长文，最后少量真实翻译。每项记录源码身份、APK 哈希、手动或 ADB 操作、结果及本地证据路径；私聊截图和 Key 不提交。源码会在准备、发送和结果回调前重读标题；标题结构和同窗口切换时序仍需真机确认，未确认身份时不能沿用旧草稿。

## 2026-09-22 复盘状态校正（实现版本 8 之前，无新增真机测试）

版本 7 已有 Windows 构建、同签名覆盖安装和取字/设置隔离记录。下文“待测”表为测试前矩阵，不再表示该版本尚无 APK；未完成的行为仍需逐项复测。后续代码方案与新增验收编号见 [真机复盘与代码调整方案](2026-09-22真机复盘与代码调整方案.md)。

- G01 为部分观察，尚未验收两位偶像的分组正确性。G02～G04 操作证据不完整；ADB 疑似穿透与执行记录中的“用户手动正常”需并列保留，状态为自动化受阻/待定位，不能推广为人工触控必然失败。
- B01～B05 正式矩阵未完成；附近显示存在快速回退异常观察。尚未复核原始日志、触发入口及译文来源，不把 show/close 顺序直接当作无空间的根因，也不当作进程崩溃。
- 当日未确认发送真实请求与附近按钮操作的关系需在原测试机核实；版本 6 真实 API 通过不转记为版本 7 通过。
- 自动预选是新增目标。现有分组建议可以在未选时展示，但翻译请求仍使用人工已选内容；没有自动正文推荐。
- 本机仅复核已提交记录、源码并重跑 11 组桌面测试，退出码 0；未取得当晚原始截图/XML/日志，未进行 Android 构建、手机操作或真实 API 请求。

## 2026-09-21 今日实验总结与自动选择需求

- 今日版本 7 已安装并运行；构建与桌面逻辑测试证据见 [优化执行记录](优化执行记录.md) 顶部。应用服务正常绑定，未发现进程崩溃。
- 实际观察：取字探针和原文整理卡可以出现，候选包含作者、时间、短消息和长文；所有候选默认未选中。当前必须人工勾选正文后才能启用“准备中文翻译”，分组复选框只作用于人工已选片段。
- 用户明确提出的新目标是“自动选择应翻译的正文，同时仍允许人工取消/追加”。该目标尚未实现；当前 `CandidateSelectionSelfTest` 的“no automatic selection”正是旧设计行为，不能作为新目标通过证据。
- “贴近原文显示”今日表现为快速出现后被移除。ADB 日志显示 `BubbleTranslationOverlay.show` → `close`，没有 `FATAL EXCEPTION`；记录为附近布局回退失败，不记录为应用崩溃。用户要求暂不修改代码，故保持现状并待后续修复。
- 今日未完成：自动正文筛选、真实正文选择后的翻译、附近译文对应、长文/密集布局、滚动/切页/旋转/键盘/锁屏和拖动入口完整矩阵。

## 2026-09-21 接手、安装与合成页实测

- 源码：`codex/chat-bubble-layout` @ `ce7582b200582ac00e30c2173dd36a5ea9ca46ef`，接手前后工作区干净。
- APK：`app/build/outputs/apk/debug/app-debug.apk`，version `0.6.0-bubble-trial (7)`，SHA-256 `8A7D27D26D76F9C2BDB2A37BA24F8DEB25D0174A7A3E6679575E09D99B955AE9`；v2 签名、权限和 OCR 模型静态检查通过。
- 设备 `RFCT51QPXEP`（Samsung SM-S9060）已由 `adb devices -l` 核实为 `device`。原安装为版本 6、同一 v2 签名；使用 `adb -s RFCT51QPXEP install -r -d app\\build\\outputs\\apk\\debug\\app-debug.apk` 覆盖安装成功，未卸载、未清除数据。安装后 dumpsys 核实版本 7/名称/签名一致，无 `FATAL EXCEPTION`。
- 已启动首页并确认无障碍服务列表包含 `TranslatorAccessibilityService`。启用“本页合成测试”后，取字探针可见；点击探针出现“原文整理 · 0.6.0-bubble-trial”候选卡，显示“已选择 0 个片段”，并展示“按聊天列表结构合并”及“准备中文翻译”等控件。证据：本地 `device-v7-home.png`、`device-v7-synthetic-enabled.png`、`device-v7-after-probe.png`。
- G01～G04：部分通过/未完成。候选卡出现证明合成采集与分组入口可达；但对候选卡整组选框的 ADB 点击未稳定命中卡片，触控随后穿透到首页“保存目标包名”并关闭卡片（Toast 显示目标包名已保存）。因此整组勾选、取消中间行、关闭合并及旧确认失效不能记为通过。该次操作未调用真实翻译、未读取私人聊天，日志未见崩溃。
- B01～B05：未执行。没有真实 Key，也未进入用户指定的私人聊天样本；真实气泡坐标、附近/完整切换、滑动/旋转/锁屏/键盘和拖动入口均不以合成页冒充通过。

### 真实聊天页追加（2026-09-21）

- 用户确认已准备并指定当前前台页面；ADB/XML 核实包名为 `jp.co.sonymusic.communication.sakurazaka`，页面标题为小田倉麗奈。证据保存在本地 `device-current-v7.png`、`device-current-v7.xml`，不提交 GitHub。
- 重新观察页面后点击取字探针：成功出现版本 7 的“原文整理”卡片；显示“已选择 0 个片段”，可见分组入口，候选中区分出作者、时间等可能辅助信息，并识别出短消息和长文。证据：`device-v7-real-candidate.png`。
- 触控阻塞复现：在候选卡区域执行一次滑动后，候选卡消失且底层聊天页响应；此前点击候选区域也曾穿透到首页保存按钮。候选卡无法安全完成正文勾选，故没有确认或发送真实翻译；未观察到应用崩溃。此问题阻塞 G02/G04 及所有需要选中正文的 B 项。
- G01：部分通过（真实页面分组入口与候选边界可见，但未完成选择结果核验）。G02/G03/G04：失败/未完成（候选窗口触控穿透，无法安全选择和逐项调整）。B01～B05：未执行；没有因该触控问题发起真实 API 请求。
- 独立检查补充：`accessibility_enabled=1`，绑定服务包含 `TranslatorAccessibilityService`，`Crashed services:{}` 为空。通过应用内入口打开 DeepSeek 设置页后，Key 输入框为密码类型且不回显；设置页未出现取字探针或原文卡。未删除/修改用户 Key，未发起网络请求。C08 设置隔离记为通过（本次观察）。

## 测试前矩阵：0.6.0-bubble-trial / 7（2026-09-21 历史快照）

交付分支 `codex/chat-bubble-layout`，实际交付身份见 [优化执行记录](优化执行记录.md)。下表保留测试前状态；版本 7 后续构建、安装和部分实测见本文上方记录，当前复盘结论以上方校正为准。以下版本 6 的通过结论不得移用。

| 用例 | 操作与通过标准 | 状态 |
| --- | --- | --- |
| G01 自动分组 | 两位偶像页面对比整组原文；同条多行可合并，不跨列表项/时间，重复句各自保留；未知结构退回片段 | 未执行 |
| G02 选择可修正 | 整组选中后取消一行，追加 OCR 保持选择；关闭合并恢复片段，变更使旧确认失效 | 未执行 |
| G03 分组边界复审 | 在可复现的合成/目标页分别核对未知作者、带字/无字图片和嵌套列表；前后正文不得错误合并。与正文无关的侧栏时间、其他窗口内容不应打断正确分组 | 未执行；桌面合成节点用例通过不代替真机 |
| G04 批量与部分选择 | 整组选中/取消、部分选中后再整组选中、取消中间一行；最终勾选与确认原文一致，不跨被取消行合并，不重复请求；切页后的旧组操作无效 | 未执行；原子选择模型测试已覆盖，Android 控件回调待测 |
| B01 附近/完整切换 | 有效 Key 翻译后点“贴近原文显示”；中文编号对应，点“返回译文”可看完整卡片，切换不产生新请求 | 未执行 |
| B02 空间与字体 | 窄屏、密集消息、长文、放大字体、分屏和屏幕边缘：不出屏、不截断、不覆盖已知原文/媒体/控件；放不下时完整卡片仍可读 | 未执行 |
| B03 位置失效 | 附近层可穿透滑动；滚动/切页/锁屏/旋转清理；仅弹出/收起键盘（不输入文字）也清理旧位置 | 未执行 |
| B04 窗口生命周期 | 显示/返回/关闭约 20 次，不重复窗口、不误关新卡片，设置页无附近译文；不复用旧页回调 | 未执行 |
| B05 拖动入口避让 | 有附近译文时拖动入口超过 6dp：附近层清除，完整译文恢复；松手不触发取字。轻触入口仍返回译文，阈值内抖动不移动按钮；拖动卡片标题正常。全过程不增加翻译请求 | 未执行；代码接入及类型编译通过，未运行 Android 手势 |

构建入口：`scripts/verify-pre-device.ps1 -ExpectedVersionCode 7 -ExpectedVersionName '0.6.0-bubble-trial'`。保留原机签名后核对 APK/设备身份，补测原 C02/C04/C06/C07/C08/C10/C11。分组可无 Key 测试；附近显示须真实译文，缺条件标未执行。附近布局最多四条，仅使用可靠节点屏幕坐标，不对 OCR 图像坐标猜映射。实际可显示数量取决于页面空隙，不以显示条数替代准确对应验收。更详细规则见 [本轮计划](CHAT_BUBBLE_PLAN.md)。

## 最新实测：0.5.0-translation-trial / 6（2026-09-19）

Windows 构建、APK 身份核对、安装和当晚真机测试已经完成。测试设备为 Samsung SM-S9060 / Android 16；助手包名为 `com.fanli.sakurazakatranslator`，目标聊天包为 `jp.co.sonymusic.communication.sakurazaka`。真实 DeepSeek 中文翻译主链路已跑通，但 C01～C13 仍有部分边界没有完整覆盖，不能笼统记为“全部通过”。下方 0.4、0.3.1 等内容是历史记录，不代表当前版本状态。

### 当晚验收结论总表

| 用例 | 状态 | 2026-09-19 实际结果 | 尚未覆盖/限制 |
| --- | --- | --- | --- |
| C01 身份/范围 | 部分通过 | ADB 确认版本名、版本码和包名；目标聊天包可前台打开；服务最终已绑定且崩溃列表为空 | 未对全部非允许 App 逐页验证浮窗隐藏 |
| C02 节点优先 | 部分通过 | 合成页与真实聊天页均能取字；真实页一次得到 20 个片段，总耗时 1210 ms | 未严格复现“先勾节点、等待 OCR 回调”全过程 |
| C03 原文保真 | 部分通过 | 日文正文进入候选；真实译文中 `💗`、`👩🏽‍💻`、花朵等符号可见并保留 | 波浪号、全部换行、同文不同位置及全文零遗漏未逐项对照；图片 OCR 不清晰 |
| C04 分组建议 | 部分通过 | 多条消息可人工选择并生成 3 条编号对应结果 | 未完整比较“开启/关闭建议分组”；节点与 OCR 尚不自动配对到同一消息 |
| C05 辅助信息 | 部分通过 | 作者名和时间在候选中可见，实际截图中正文被选、作者/时间未进入已确认正文 | 自动识别和自动排除未实现，仍依赖人工选择 |
| C06 卡片交互 | 部分通过 | 候选卡和译文卡可显示、滚动、关闭；长文本卡片可查看，未见崩溃 | 拖动边界、展开/折叠和字体调整未形成完整证据 |
| C07 旧页失效 | 部分通过 | 锁屏后浮窗清除；解锁后旧确认和旧译文未恢复；重启后无旧卡片 | 本次解锁后系统未自动恢复助手无障碍服务，需用户手动重开；翻译中切页/取消未完整实测 |
| C08 设置隔离 | 通过（本次样本） | 设置页焦点为 `TranslationSettingsActivity`；Key 为密码输入且不回显；未出现取字浮窗或旧译文 | 未做系统级截图拦截的独立攻击性验证 |
| C09 Key 生命周期 | 部分通过 | 删除 Key 后明确阻止发送且未观察到 DeepSeek 连接；重新输入后可恢复真实翻译 | 留空保存、重启后解密使用和设备迁移未全部单独验证 |
| C10 原文确认 | 部分通过 | 准备阶段未观察到联网；缺 Key 时保留原文并提示；显式发送后才产生真实请求 | 改选择/改分组使旧确认失效未完整复现 |
| C11 实际中文 | 通过（主链路） | USB 观察到连接 `api.deepseek.com:443`；用户确认服务商侧产生费用；抓屏看到编号对应中文结果及符号保留 | 未覆盖两份风格对比、所有长文逐段对照及完整 HTTP/额度矩阵 |
| C12 网络错误 | 未完整执行 | 缺 Key 路径提示清楚、无自动请求；恢复 Key 后可显式重发 | 错 Key、断网、慢网、超时、限流和额度不足未实测 |
| C13 连续使用 | 部分通过 | ADB 连续打开/返回助手 10 次均完成；最终 `Crashed services:{}`，未见助手崩溃 | 未达到约 20 次完整取字/选择/付费翻译循环，未覆盖旋转、字体和内存趋势 |

总体判断：当前版本已达到“读取聊天文字 → 人工选择 → 保守分组 → 确认发送 → DeepSeek 中文翻译 → 中文结果卡片”的试用主流程。人名/时间仍是候选而不是自动排除项；当前结果是独立卡片，不会贴回原聊天气泡；图片内文字识别质量不足，本轮按用户决定暂缓。

## 2026-09-17 新版待验证

当前源码 `0.4.0-probe-candidates` / 5。七组桌面逻辑测试已通过，但本机没有 Android SDK，尚未构建、安装或测试新版 APK；没有新版哈希。Windows 构建步骤和 C01～C13 真机矩阵见 [本轮优化验收与交接](本轮优化验收与交接.md)。重点检查节点先选后 OCR 不重置、卡片不被自身事件误关闭、切页清理、迟到回调及图片释放。以下测试记录保持为旧版本证据，不补记为新版通过。

> 2026-09-16 首轮目标页面结果、证据边界和后续修复建议见 [2026-09-16 真机测试结果与后续修复建议](2026-09-16真机测试结果与后续修复建议.md)。截图/OCR/候选链路已获得本轮视觉证据；Emoji 保真不通过，完整生命周期矩阵仍未执行。

## 2026-09-16 本轮测试（已有首轮视觉证据，完整矩阵未执行）

源码版本标记 `0.3.1-probe-review` / 4。本轮已在 Windows PowerShell 5.1 下通过七组逻辑测试、Android Java 编译、lint、assembleDebug 和 APK 静态检查。用户随后提供三份目标页面结果截图；ADB 在报告整理时已断开，因此设备端版本身份和完整真机矩阵尚未确认。下面 2026-09-14 截图是旧版本的有效历史证据，不能与本轮结果混用。

- 路径：`D:\Projects\sakurazaka-translator\app\build\outputs\apk\debug\app-debug.apk`
- 大小：`53,400,368` 字节
- SHA-256：`463CE6B4EAD735B07EF442B5DE09B2BF551A614517F18FA7AA95D9DA22C6E249`
- 静态结论：包名和版本正确；v2 签名有效；无 `INTERNET` / `ACCESS_NETWORK_STATE`；包含日文 OCR native pipeline 与 bundled 模型。
- lint：构建通过；保留 Gradle 新版本提示和一个未使用字符串警告，不阻塞真机测试。
- 安装状态：用户已进入目标页面并运行当前样式的结果卡片；截图未显示版本/哈希，ADB 已断开，因此记为“操作链路可用、安装身份未独立确认”。未自动卸载旧包或清除应用数据。

复测记录模板：日期 / APK 版本与哈希 / 手机和 Android 版本 / 用例 / 步骤 / 实际结果 / 通过或失败。不要将订阅正文、截图或设备序列号提交到公共仓库。

## 2026-09-14 新版目标页视觉证据

来源为用户在本次会话提供的两张系统截图。这里只记录行为结论，不保存订阅正文、设备序列号或外部图片副本。截图界面与当前结构化探针一致，但没有显示 APK 版本或哈希，因此不能单独证明安装包身份。

| 页面/路径 | 已观察结果 | 当前结论与缺口 |
| --- | --- | --- |
| 目标长消息／节点 | 指定窗口截图成功，尺寸 `1080×2340`；节点描述读取到可见长消息段落，换行仍在 | 部分通过；作者、时间、正文、来源标签和坐标仍混排，单张截图不足以证明零遗漏、无截断或重复处理正确 |
| 多条消息与语音页／OCR | 输出 13 个 OCR 块；正文、作者/时间及 `00:25` 时长均有结果；一次样本总耗时 `836ms` | OCR 通路通过，正文质量部分通过；辅助信息混入，部分符号被识别为多余字符，尚无候选选择与来源分类 |
| 结果卡片 | 截图预览与文字结果同时可见，“关闭”按钮可见 | 可展示证据；本组截图没有覆盖拖动、关闭后迟到回调、切页、锁屏和服务重启 |

据此，目标长消息不再记为“完全无证据”，但 P0.5 仍未通过。下一轮需先落地 M2/M3，再用新包对照原页面复测：正文/元数据分类、同句重复、边缘截断、OCR 候选选择、切页和锁屏失效。

### 依据本次证据生成的待复测包

- 路径：`D:\Projects\sakurazaka-translator\app\build\outputs\apk\debug\app-debug.apk`
- 版本：`0.3.0-probe-select`（versionCode `3`）
- 大小：`53,383,422` 字节
- SHA-256：`443F2341C476E4180C7E39CCA336B4E6F84C0C99C12C2C79E3E3308F456D9D63`
- 静态验证：逻辑测试、Java 编译、lint、assembleDebug、v2 签名、权限和日文 OCR 模型检查均通过。
- 安装状态：2026-09-14 已通过 ADB `install -r` 成功覆盖安装；设备端确认 versionCode `3`、versionName `0.3.0-probe-select`；原无障碍服务配置仍在，助手进程可启动。
- 待手机确认：正文分区不误分长文；坐标不再遮挡阅读；OCR 候选默认不选、可逐项勾选并正确汇总；OCR 更新不重置滚动位置；关闭、切页和锁屏仍能使旧结果失效。

## 2026-09-13 规划交接

本次未检查 ADB、未安装、未截图或执行手机用例。旧安装和视觉证据继续按下表保留，不能当作新版本验收。下一轮按 [方案 2.0](PROBE_TEXT_OPTIMIZATION_HANDOFF.md) 第 9 节：先 Android 适配与合成测试，再目标四类页面；T01～T25 的桌面、Android 适配、目标正文证据分开记录。缺长文/重复/截断样本保持待验证。

补充：本轮已检查 ADB，当前无设备；已生成待真机复测 APK `D:\Projects\sakurazaka-translator\app\build\outputs\apk\debug\app-debug.apk`，版本 `0.2.0-probe-text`/2，SHA-256 `A91A4E66C7A1140CBD165054C7C8D4710AE9F41CB8FBCB8B84EE84F11C4ED5FA`。安装、授权、截图、节点正文、OCR 和目标页面仍待手机证据。

## 最新证据汇总（2026-09-12）

来源是本次会话中的安装输出、用户操作反馈及截图，不保存原始订阅正文或设备序列号。

| 项目 | 已观察情况 | 仍未验证/已发现问题 |
| --- | --- | --- |
| 安装、启动、授权 | 已有安装 Success、启动及服务使用证据 | 新优化版尚未生成 |
| 卡片 | 用户确认持续显示、滚动和自主关闭 | 拖动边界、切页/锁屏/销毁需系统回归 |
| 合成截图与图片 OCR | 当前页面截图及两句自绘日文已显示 | 完整合成矩阵、断网、异常路径待测 |
| 合成重复句 | OCR 截图中两句同时出现 | 不等于所有结构去重场景通过 |
| 目标普通消息节点 | 描述字段保留花、爱心、换行 | 仅限已展示样本，不能证明所有页面完整 |
| 目标 OCR | 多消息/图文混排/短界面可识别 | 漏符号、错汉字、错序、辅助信息混入 |
| 目标长文/截断/重复句 | 2026-09-14 已有一份节点长消息正向样本 | 截断、重复句及零遗漏仍无充分证据 |

目标包已由设备前台确认：`jp.co.sonymusic.communication.sakurazaka`。短界面不等于短消息测试，照片和文字混排不等于照片内文字测试。P0.5 仍未通过。

下一步按 [正文整理版交接](PROBE_TEXT_OPTIMIZATION_HANDOFF.md) 开发并执行 T01～T19；先手机合成，再目标回归。用户可用系统截图反馈；ADB 曾在线后断开，不能将后来断开解释成此前未安装。

## 以下为历史测试记录及旧矩阵

下方“尚未开始/未安装”仅为当时记录；没有勾选的历史矩阵不得自动补成通过。新版本重新填写实际执行证据。

尚未开始。记录时只填写设备型号、Android/One UI 版本、目标 App 版本、助手版本、页面类别、来源、是否完整、耗时和错误码，不填写设备序列号或原始聊天正文。

待安装 APK：`D:\Projects\sakurazaka-translator\app\build\outputs\apk\debug\app-debug.apk`；SHA-256：`FBCE49974E826F80E6EF915C4DFAD9474E1D6120400F3CB6387033784DA847DD`。ADB 在 2026-09-12 最后检查时无设备，因此以下项目均未执行：安装、无障碍授权、合成页截图/OCR、目标 App 四类页面。

## 当前测试前提

以上 APK 为已有构建检查点。正式返测前完成方案第 28.7 节 A1～A5，替换新包并记录新哈希；本次仅规划，没有执行手机测试。

## 合成自测矩阵（B）

| 用例 | 预期 | 状态 |
| --- | --- | --- |
| 安装、启动、无障碍授权 | 安装成功，服务可开启，应用无崩溃 | 待测 |
| 日文短句和换行长文 | 可见完整句子无整块遗漏，顺序可核对 | 待测 |
| 同屏两个相同短句 | 两条均保留；不同位置不合并 | 待测 |
| 自绘图片文字 | 节点不含图片句，OCR 可识别可见图片句 | 待测 |
| 断网 OCR | bundled 模型能完成识别；记录字级差异 | 待测 |
| 截图及长结果 | 预览对应当前窗口，正文可完整滚动查看 | 待测 |
| 连点、取消后再点、关闭卡片 | 无并行物理任务、旧结果覆盖或卡死 | 待测 |
| 目标滚动及允许页面之间切换 | 旧结果失效；卡片内部滚动不误取消 | 待测 |
| 后台、锁屏、服务停用/重启 | 悬浮窗口清除，迟到回调不重现；重启可用 | 待测 |
| 截图失败 | 错误码可见，节点正文仍可独立核对 | 待测 |

只比较当前可见内容；必要时分屏滚动分别取样。失败记录助手版本、复现步骤、预期/实际差异和错误类别，不保存敏感正文。

## 目标页面矩阵（C，B 通过后执行）

| 页面 | 节点路径 | 截图/日文 OCR 路径 | 正文结论 |
| --- | --- | --- | --- |
| 短消息 | 待测 | 待测 | 未通过验收 |
| 多条同屏，含重复短句 | 待测 | OCR 多条消息部分通过；重复短句未测 | 未通过验收 |
| 长消息及边缘截断 | 节点长消息部分通过；边缘截断未测 | 待测 | 未通过验收 |
| 图片混排 | 待测 | 待测 | 未通过验收 |

每格记录：遗漏数、误去重数、顺序差异、媒体文字混入、字级错误、截断提示、耗时及错误码。先从设备证据确认目标包再测试；至少一条路径在四类页面达标后选定主路径，未测项不得记为通过。

## 最新待安装产物（2026-09-12）

此前记录的 APK 哈希仅属于历史构建检查点；本次 A1～A5 收尾后的产物为：

- 路径：`D:\Projects\sakurazaka-translator\app\build\outputs\apk\debug\app-debug.apk`
- 包名：`com.fanli.sakurazakatranslator`
- 版本：`0.1.0-probe`（versionCode `1`）
- 大小：`53,683,519` 字节
- SHA-256：`1B8C9B66EFE45189B05E382DFE54E2A7839D3C250B39E81062034B4E1A676799`

### 2026-09-12 合成页复测记录

- 结果卡片已稳定显示，可滑动并由用户关闭。
- 截图与当前合成页一致，显示 `1080×2340`。
- 图片区域两句日文均出现在 OCR 结果中：`画像内だけの日本語です。`、`OCR で二行目も確認します。`。
- 中日混排时中文 OCR 有误识别，暂记为未完全通过。
- ADB 状态：无设备；安装、截图、OCR 正文和真机验收待执行。

安装后先打开应用，勾选“启用本页合成测试”，开启无障碍服务，在内置合成页点击“取字探针”。只有合成页正文通过后，才保存由设备前台证据确认的目标包名并进入四类目标页面测试。

## 2026-09-19 Windows 接手与新版构建

- 源码：`codex/node-candidates @ dfbcded1bfd70b834393d905e9047dd5c1eee459`。
- APK：`app/build/outputs/apk/debug/app-debug.apk`；`0.5.0-translation-trial (6)`；SHA-256 `7B32101BC4B19A5C1111B602395F4CDF71D4EE5485035F070C94DD3C1ADE4778`；v2 签名通过。
- 构建：桌面逻辑测试、Android 编译、JUnit、lint、assembleDebug 和产物检查均通过；命令与证据详见 `docs/优化执行记录.md`。
- 设备：接手时首次检查 `adb devices -l` 无设备；随后用户连接 SM-S9060 并继续执行了本版 APK 真机测试。该行只记录测试开始前状态，最终结论以本文顶部总表及下方真机记录为准。

### 当晚执行时间线与证据边界

1. 用户通过 USB 连接 Samsung SM-S9060（Android 16）并自行安装 APK。ADB 随后确认设备端包名、`versionName=0.5.0-translation-trial`、`versionCode=6`，安装更新时间为 `2026-09-19 22:29:27`；未卸载旧包、未清除应用数据。
2. 无障碍设置最初显示已启用，但系统服务列表曾把助手列入崩溃服务。用户按提示关闭再开启后，服务成功绑定，崩溃列表恢复为空，“取字探针”浮窗出现。后续大部分检查结束时均再次确认 `Crashed services:{}`。
3. 合成页点击探针后，日志出现 `OCR process succeeded via visionkit pipeline`，候选卡正常出现，候选默认不自动发送。第一次发送因没有可用 Key 显示缺少 API Key，原文仍保留，未见自动重试。
4. 用户只在手机设置页输入 Key。随后 USB 观察到本应用连接 `api.deepseek.com:443`，用户同时确认 DeepSeek 侧发生计费；译文卡显示编号对应的中文，并保留可见组合 Emoji。Key 本身、Authorization、服务端响应原文和私人聊天内容均未写入文档。
5. 真实聊天页依次完成多条消息、图文混排和长文本观察。多条消息取字可用，但作者、时间、按钮和 OCR 片段仍会一起进入候选层；当前依靠人工勾选正文。图文混排的聊天气泡文字能翻译，图片内部文字识别不清晰。长文本能生成并滚动查看，但未逐段完成全文零遗漏对照。
6. 后续补测设置隔离、删除 Key、恢复 Key、锁屏/解锁、应用重启、目标包切换和 10 次无费用打开/返回循环。删除 Key 后未见 DeepSeek 请求；恢复 Key 后再次得到译文。锁屏能清除旧浮窗和旧内容，但本次解锁后无障碍服务需要用户手动重新开启。

本机证据文件为工程根目录下的 `device-*.png` 和两份 `device-*.xml`，均为本地未跟踪文件，不提交到 GitHub。它们包含设备界面信息，仅用于本轮核对；“通过”结论只采用当晚实际看到的行为，不用历史截图替代。

## 2026-09-19 真机复测（SM-S9060 / Android 16）

- C01 基础身份：部分通过。ADB 核对包名 `com.fanli.sakurazakatranslator`、`versionName=0.5.0-translation-trial`、`versionCode=6`；无障碍服务关闭后重新开启，服务成功绑定且 `Crashed services` 为空。
- C02/C03 取字与原文候选：部分通过。合成页点击“取字探针”后，浮窗和原文整理卡片出现；日志记录 `OCR process succeeded via visionkit pipeline`；候选默认未选，可滚动查看；本次截图未逐项完成全部符号核对。
- C10 原文确认/发送边界：部分通过。准备阶段显示候选和“准备中文翻译”，未观察到 DeepSeek 请求；未配置 Key 时发送显示“缺少 API Key”，未崩溃、未自动重试。
- C11 实际中文翻译：未执行，缺少用户在手机设置页输入的有效 Key。
- C11 后续观察：USB 日志于 `2026-09-19 22:45:41` 显示本应用 UID 通过 `api.deepseek.com:443` 发起连接；应用进程仍在运行、无崩溃。抓取屏幕时翻译卡片已被关闭，因此本次只确认“真实请求已发起”，未确认该次响应正文、模型、额度或符号质量。
- C11 重试结果：用户确认已在手机设置页输入有效 API Key，并在 DeepSeek 侧看到本次请求已扣费；USB 日志同时显示本应用连接 `api.deepseek.com:443`。抓屏确认翻译卡片重新显示两条对应结果；原文编号、中文结果和 `💗`、`👩🏽‍💻` 均可见，服务未崩溃。本次真实中文翻译记为通过；HTTP 状态码和服务端额度明细未写入应用日志，仍不记录为完整错误/额度矩阵通过。
- C04、C05、C06、C07、C08、C09、C12、C13：首次联调时尚未完整执行；当晚后续已补测其中部分核心路径，最终状态见本文顶部总表。

### 多条真实消息首次取字（2026-09-19）

- 目标页面抓取卡片显示 `20 个片段`，采集总耗时 `1210ms`；无障碍服务仍绑定，未出现崩溃。
- 一条可见日文正文成功出现在候选中；同屏还出现作者名、日期时间、返回符号和 OCR 候选。为避免保存私人聊天内容，不在文档复述正文。
- 卡片明确提示“可能缺少或误认表情；作者、时间仍需手动排除；已有节点原文时避免重复选择；两种来源尚未自动匹配消息”。
- 当前结论：多条消息取字与人工候选链路部分通过；自动作者/时间排除、节点/OCR 自动配对和消息级分组仍未通过验收，需继续按 C04/C05 人工选择后再翻译。

### 图文混排实验首次结果（2026-09-19）

- 截图显示翻译卡片生成了 3 条带编号的中文结果，原文/译文对应关系可见；花朵 Emoji 与爱心均保留，服务无崩溃。
- 本次可见结果主要是聊天气泡文字；截图和现有输出未能确认图片内文字是否进入 OCR 候选，因此图文混排中的图片 OCR 仍记为未确认，不将本次结果当作图片文字通过证据。

### 长文本测试结果（2026-09-19）

- USB 抓屏显示长文本中文结果卡片正常显示，多个连续段落可见并可滚动；无障碍服务仍绑定、`Crashed services` 为空，未观察到应用崩溃。
- 当前只能确认“长文本结果生成并可查看”；单张截图未覆盖全文逐段对照，截断、漏段和完整顺序仍需滚动对照后再判定。
- 图片内文字识别：用户确认当前识别不清晰，因使用频率低，本轮暂不修复；保留为已知限制，后续如需求增加再单独设计图片 OCR/视觉识别方案。
- C07 锁屏/解锁自动检查：锁屏期间浮窗被清除，解锁后旧卡片和旧译文未残留，应用无崩溃；本次设备在解锁后未自动恢复本应用无障碍服务，需要用户手动重新开启。服务重新开启后 USB 核对为已绑定、`Crashed services` 为空。该行为暂记为设备系统/服务生命周期限制，后续可单独复测是否与强制停止应用有关。
- C08 设置隔离：通过 USB 核对设置页为 `TranslationSettingsActivity`；API Key 输入框为密码框，页面显示“已保存加密 Key；不会回显”，未出现取字浮窗或旧翻译卡片，无崩溃。随后已继续执行 C09 的删除与恢复路径；重启后解密、留空保存和设备迁移仍未单独验证。
- C09 Key 删除：用户删除已保存 Key 后发送一条已选正文；屏幕提示“请先在助手首页设置 API Key。原文仍保留；重新发送可能再次计费。”，USB 最近日志未发现 `api.deepseek.com` 请求，服务无崩溃。删除后阻止网络请求通过。
- C09 Key 恢复：用户重新输入 Key 后发送短消息，USB 抓屏确认新的中文译文卡片显示，原文/译文对应正常；无障碍服务绑定、`Crashed services` 为空，未发现应用崩溃。
- C13 无费用稳定性循环：通过 ADB 连续打开/返回应用 10 次；每次均完成，最终 `Crashed services:{}`，未发现应用进程崩溃。日志中的 `AndroidRuntime` 仅为 ADB `monkey` 工具正常启动，不是目标应用异常。未重复发送翻译请求，避免额外 API 费用。

### 目标应用范围过滤复核（2026-09-19）

- 通过 USB 启动目标包 `jp.co.sonymusic.communication.sakurazaka`，窗口焦点切换到目标应用；返回助手后焦点恢复为 `com.fanli.sakurazakatranslator.MainActivity`。
- 返回后无障碍服务仍已绑定，`Crashed services:{}`；未发现助手在非目标页面产生新的翻译卡片或崩溃。
- 结论：目标应用范围过滤完成独立复核；未修改源码。

## 2026-09-22 新版接手检查（无设备）

- 源码身份：`4e7ab418aa99105135483d470d3712317337e6b9`；构建后工作区另有两处为编译修复产生的未提交源码差异。
- ADB 命令：`D:\Android\Sdk\platform-tools\adb.exe devices -l`；实际结果仅有 `List of devices attached`，无设备行。
- 结果：未执行安装、启动、无障碍服务、合成阅读卡、真实聊天、R/L/W/Q/I、缺 Key、成员切换、截图/XML 或真实 API 测试；均待设备连接并按授权条件人工/自动复核。
- 本次可确认范围：离线桌面逻辑与 Android 构建通过，不等同于真机通过或真实 API 通过。构建 APK 本地证据：`app/build/outputs/apk/debug/app-debug.apk`，SHA-256 `8438C13C3729959B01F274FBBA57C73248A569954510815C8C64456751392C14`。

## 2026-09-22 设备接入后复测

- 设备：Samsung SM-S9060，ADB serial `RFCT51QPXEP`；未在文档记录序列号以外的私人设备信息。通过 `adb install -r` 覆盖安装新版 APK，未卸载、未清数据。设备端确认 `versionCode=8`、`versionName=0.7.0-reading-card-trial`。
- 安装后系统关闭了无障碍服务；用户手动重新开启后，`dumpsys accessibility` 确认服务已启用且 `Crashed services:{}` 为空，应用进程正常。
- 首页合成样本控件树和本地截图显示正常，合成测试复选框保持开启，浮动入口“翻译当前屏”出现。证据保留在本机临时目录 `C:\Users\FanLi\AppData\Local\Temp\sakurazaka-home.xml`、`sakura-current2.png`。
- 通过 ADB 两次点击浮窗坐标并等待 OCR，候选卡未出现；无崩溃日志，服务仍绑定。ADB 点击不证明触摸响应，故取字/候选卡记为“未确认/待人工触摸”，未进入阅读卡、真实聊天或真实 API 测试。

## 2026-09-22 单次真实 API 尝试

- 用户在手机配置 API Key，并人工确认当前页面成员；候选卡保留一条短消息选择。仅点击一次“准备中文翻译”，未连续重试。
- 应用日志出现 `api.deepseek.com` DNS 解析及 TCP 连接；应用进程和无障碍服务均未崩溃。未记录私人正文内容。
- 请求后截图显示候选卡重新出现，但没有可确认的中文结果、HTTP 状态或服务端错误。证据保留在本机临时目录 `C:\Users\FanLi\AppData\Local\Temp\sakurazaka-after-prepare.png`、`sakurazaka-result.png`。
- 结论：真实 API 请求已发起/产生网络连接；真实翻译结果未确认；未重试，避免重复计费。响应回调、结果卡片和阅读卡切换仍待验证。

## 2026-09-22 最终真机结论与问题清单

- 真机取字人工触摸后候选卡出现，标题为 `原文整理 · 0.7.0-reading-card-trial`，显示 20 个片段；候选初始未勾选，正文仍需手工选择。
- 成员自动识别未达到预期：页面显示“尚未确认当前成员”，仍需人工选择/确认风格。人工确认后门禁解除，但不等于自动识别通过。
- 界面需要美化：候选卡信息密度高，正文、作者、时间和 OCR 辅助信息混在同一滚动层；按钮层级不够清晰；浮窗/候选卡覆盖目标页面明显；触摸和 ADB 坐标操作存在不稳定或穿透风险。
- 免 API 阅读卡入口虽存在源码，但本次真机候选卡中未稳定显示；阅读卡完整滚动、切换、展开/收起、关闭重取等未完成可靠验收。
- 自动排除作者/时间/按钮、长文补取和真实 API 中文结果均未通过；未重复收费请求，未提交私人聊天内容、截图或 XML。

## 2026-09-22 其余自动稳定性检查

- ADB 冷启动助手、切换到目标 App、返回目标 App 后保持无障碍服务绑定；版本仍为 `0.7.0-reading-card-trial (8)`，`Crashed services:{}` 为空。未新增 DeepSeek 域名连接。
- 通过 ADB 直接启动非 exported 的 `TranslationSettingsActivity` 被 Android 拒绝（`SecurityException: ... not exported`）；这是系统入口保护，不能作为应用崩溃，设置页需从应用首页按钮进入，未修改 Key 或设置数据。
- 真机可自动完成的启动/切换/服务/崩溃范围已完成；候选卡触摸穿透、阅读卡滚动手感、视觉舒适度、真实 API 响应正文、成员风格是否主观合适仍需人工确认或已有页面再次操作。
