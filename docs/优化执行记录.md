# 优化执行记录

## 2026-09-21 今日实验详录与需求差异确认

### 实验身份与范围

- 源码：`codex/chat-bubble-layout` @ `ce7582b200582ac00e30c2173dd36a5ea9ca46ef`；APK：`0.6.0-bubble-trial` / versionCode 7，SHA-256 `8A7D27D26D76F9C2BDB2A37BA24F8DEB25D0174A7A3E6679575E09D99B955AE9`。
- 设备：Samsung SM-S9060，ADB serial `RFCT51QPXEP`。APK 由版本 6 同签名覆盖安装，未卸载、未清除数据。无障碍服务已启用并绑定，测试期间 `Crashed services:{}` 为空。
- 用户已在手机端准备真实 Key 和指定聊天页面；Key 未通过命令行读取、未写入记录，未记录私人聊天全文；今日没有确认发送真实翻译请求。
- 今日实际保存的本地证据文件包括 `device-current-v7.png/xml`、`device-v7-real-candidate.png/xml`、`device-v7-real-scroll.png`、`device-v7-home.png/xml` 等；私人截图/XML仅留在本机，不提交 GitHub。

### 已完成步骤与结果

1. Windows 构建及桌面逻辑测试全部通过：分组 39、选择 60、布局 344，Android 编译、JUnit、lint、assembleDebug 和 APK 静态检查退出码 0。
2. 覆盖安装版本 7 成功；包名、版本、v2 签名一致，无应用崩溃证据。
3. 首页合成页：取字探针可显示，原文整理卡可显示；候选默认显示“已选择 0 个片段”。
4. 真实聊天页：取字探针可显示；原文整理卡能看到作者、时间、短消息和长文候选，默认全部未选中；未发送网络请求。
5. 设置页：通过应用内入口打开；Key 输入框为密码类型且不回显，设置页没有取字探针或旧译文卡；未修改或删除 Key。
6. 附近显示：用户反馈“贴近原文显示”闪退、速度过快看不清。ADB 日志核实不是进程崩溃，而是 `BubbleTranslationOverlay.show` 后约 40ms 内 `BubbleTranslationOverlay.close`，随后 Toast 提示附近空间/原文位置不足。应用进程仍存活，服务仍绑定。

### 今日出错与限制

- 合成页阶段，ADB 固定坐标点击候选卡曾穿透到底层首页保存按钮；真实页面阶段，ADB 滑动候选卡后卡片消失、底层聊天页面响应。用户随后说明手动操作可以正常进行，因此该现象暂定为“ADB 坐标/窗口时序与人工操作不一致”，不能直接判定人工触控失败；但仍未获得稳定的自动化证据。
- 附近显示反复创建/移除窗口，日志没有 `FATAL EXCEPTION`、`ANR` 或应用进程退出；实际表现是附近布局没有得到可放置项后立即回退，因此属于功能失败/快速回退，不应继续称为闪退。
- 今日没有完成正文候选的安全勾选、整组选择、取消中间行、确认原文、真实翻译、附近译文对应关系或重复请求检查；B01～B05 不能记为通过。

### 自动选择需求确认（重要）

- 当前程序的设计和实际行为是：所有候选默认不选；用户手动勾选正文；“按聊天列表结构合并”只是对已经选中的片段提供分组建议；OCR 追加也不会自动选中。
- 现有自测明确验证了 `no automatic selection`，这与用户今天提出的目标不同。当前版本不能实现“自动识别正文并自动选择需要翻译的部分”。
- 用户目标应单独定义为：根据可靠的作者/正文/时间/媒体/控件和消息边界证据，自动选中高置信度正文；不确定、媒体、辅助信息、未知内容保留未选并明确提示；自动选择结果仍需允许用户逐项取消或追加。不能把“自动分组建议”冒充“自动正文筛选”。
- 因此自动正文筛选是后续功能需求，不是今天版本已通过的验收项；在没有实现和专项测试前，不具备宣布完成或推进真实收费翻译扩展的条件。

### 代码变更边界

- 今日在用户要求“先不要修改”后未修改任何应用源码、测试代码或构建配置；当前工作区只有验收文档改动。附近显示问题和自动正文筛选均暂不修复，等待需求方案确认。

## 2026-09-21 Windows 接手与版本 7 构建

- 接手目录：`D:\Projects\sakurazaka-translator`；远程 `origin` 核对为 `https://github.com/fanli20040629-max/sakurazaka-translator.git`。接手前工作区干净：无 `git status --short`、diff、暂存差异或未跟踪文件；未修改 `local.properties`、`debug.keystore`。
- 远程目标分支经 `git fetch origin` 核实存在；已安全切换并跟踪 `codex/chat-bubble-layout`。最终源码为 `ce7582b200582ac00e30c2173dd36a5ea9ca46ef`（`ce7582b`），工作区仍干净；其父提交包含 `771b7b7` 新版实现，不以 `92fc7cc` 基线代替。
- 源码核对命中 `BubbleLayout`、`BubbleTranslationOverlay`、`MessageGrouper`、`CandidateSelection`、服务/面板接入，以及 `MessageGroupingSelfTest`、`CandidateSelectionSelfTest`；文档列出的 G01～G04/B01～B05 仍需真机操作，源码存在不等于行为通过。
- 工具链：JBR `D:\android_stufio\android-studio\jbr`，SDK `D:\Android\Sdk`，Gradle Wrapper 9.1.0；项目实际 compile/target SDK 36、min SDK 34、Java 源码目标 17、版本 `0.6.0-bubble-trial` / versionCode 7。
- 实际命令：`powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify-pre-device.ps1 -JavaHome 'D:\android_stufio\android-studio\jbr' -SdkRoot 'D:\Android\Sdk' -ExpectedVersionCode 7 -ExpectedVersionName '0.6.0-bubble-trial'`，退出码 0。逻辑测试、Android Java 编译、`testDebugUnitTest`、lint、`assembleDebug` 和 APK 静态检查均通过；桌面断言为分组 39、选择 60、布局 344，另有翻译/任务/回归套件 PASS。
- APK：`app/build/outputs/apk/debug/app-debug.apk`，`53,494,358` bytes，SHA-256 `8A7D27D26D76F9C2BDB2A37BA24F8DEB25D0174A7A3E6679575E09D99B955AE9`；包名 `com.fanli.sakurazakatranslator`，v2 签名通过，`INTERNET` 存在、`ACCESS_NETWORK_STATE` 不存在，日文 OCR native pipeline/model 存在。构建日志有 Android 分析文件写权限警告，但不影响任务退出码。
- 设备阶段：截至本记录写入时 `adb devices -l` 无设备；未安装、未覆盖安装，G01～G04/B01～B05 及真实 API 均未执行。等待用户连接、解锁、USB 调试授权并开启无障碍服务。
- 后续设备实测：设备 `RFCT51QPXEP`（Samsung SM-S9060）已连接并覆盖安装版本 7，保留原数据与同一 v2 签名；无障碍服务已启用。首页合成测试可显示取字探针和原文整理卡片，未调用真实 API。候选卡整组选框一次点击未稳定命中，触控穿透至首页保存按钮，故 G01～G04 仅记部分通过/未完成；未进入私人聊天，B01～B05 未执行。设备本地截图/XML 不提交 GitHub。
- 真实页追加：用户确认准备后，ADB 核实前台为 `jp.co.sonymusic.communication.sakurazaka` 的小田倉麗奈页面；取字后版本 7 原文整理卡成功显示，包含作者/时间辅助候选、短消息和长文，默认未选中。候选卡滑动事件再次穿透到底层聊天页并关闭卡片，无法安全勾选正文；未确认、未发送、未触发真实 API。该触控窗口问题是当前阻塞项，详见 `DEVICE_TESTS.md`，私人截图/XML仅本地保存。
- 独立检查补充：再次核对 `accessibility_enabled=1`、服务已绑定且 `Crashed services:{}` 为空；通过应用内入口打开翻译设置，Key 输入框为密码类型且不回显，设置页未出现取字/旧卡片。未修改 Key、未联网。

## 2026-09-21 GitHub 交付

- 用户要求上传；完整生产代码、回归测试和交接文档共 25 个文件已提交为 `771b7b70ee0450b5c1f028b70456ad5dcda01699`（`feat: add conservative chat grouping and nearby translation overlays`）。包含分组边界、批量选择、拖动入口避让及嵌套图片复审修正。
- 通过已登录的 GitHub Desktop 发布到 `origin/codex/chat-bubble-layout`；随后 `git ls-remote --heads origin codex/chat-bubble-layout` 返回与本地完全一致的上述提交。`master` 未合并本轮代码。本节为随后的文档交付记录；接手时获取该分支最新提交，并确认历史包含 `771b7b7`。
- 提交前重新运行：11 组桌面逻辑测试退出码 0（分组 39、选择 60、布局 344 检查）；32 个生产 Java 类型编译退出码 0，仅 5 条已有 recycle 弃用警告；协议/HTTP JUnit `OK (9 tests)`。这仍不等于完整 Android 构建、真实 API 或版本 7 手机验收通过。
- 暂存文件检查、Markdown UTF-8/相对链接、`git diff --cached --check` 通过。未提交 `.verification`、本地配置、签名、聊天截图或设备证据；凭据特征检查未检出候选密钥。
- Windows 接手入口：[本轮优化验收与交接](本轮优化验收与交接.md)。先 fetch 并安全切换/快进交付分支，核对实际源码，执行版本 7 构建后再安装；由接手助手操作 G01～G04/B01～B05，系统授权、解锁及 Key 输入由用户完成。
- 下方“未提交/未上传”描述修正和首次实现时的历史状态，不再代表本轮交付状态。

## 2026-09-21 复审修正（当前最新增量）

用户确认“全部修正优化”。继续在 `codex/chat-bubble-layout` 的现有工作区开发，HEAD 仍为 `92fc7cc`，版本仍为 `0.6.0-bubble-trial` / 7；修改未提交、未上传，不以 HEAD 或版本号相同推断另一台电脑已收到修正。以下结论优先于下方同日首次实现的测试计数。

### 修正内容与回归证据

- `MessageGrouper`：将未知角色、媒体、控件、未选中正文纳入分隔边界，无文字说明的图片/视频/按钮同样生效；屏障按同窗口、最近消息行和正文横向范围限定。未知边界保留片段，不修改日文、换行或 Emoji。
- 查找消息行遇内层列表即停止，不能回落到外层 section；内层列表本身的占据区域仍作为外层行的屏障，避免外层正文跨过内部图片合并。两条规则分别保护“内部消息归属”和“外部消息连续性”。
- `CandidateSelection` 增加批量入口，先校验整批 ID 和页面身份，再一次变更；返回值表示是否真的改变选择。`CandidatePanel` 同步子框时屏蔽逐片段回调，再向服务通知一次。保留单片段 API 并复用批量入口；取消旧确认和刷新仍由服务负责。
- `TranslatorAccessibilityService` 为原拖动函数增加可选开始拖动回调：首次超过 6dp 时恢复完整译文卡片，再移动入口；松手不触发取字，阈值内抖动不移动窗口。卡片标题拖动和轻触入口沿用原逻辑；此路径不新建翻译请求。
- 分组测试先在旧代码实际失败，输出未知作者、媒体、嵌套列表、无关时间和取消中间行五类回归失败；随后修正并通过。批量接口测试先因缺少 `List<String>` 入口编译失败，再实现并验证原子性、无变化、重复 ID、OCR 追加和过期页面。
- 独立复审另复现“外层正文 → 内层列表里的无字图片 → 外层正文”仍被合并。新增正式回归先失败（`outer text must not skip media hidden inside a nested list`），修正列表区域屏障后通过；临时复现只使用合成数据，未写入私人聊天证据。

### 本机实际检查

- JDK：`/Applications/PyCharm.app/Contents/jbr/Contents/Home`，JBR 25.0.3；Java 源码目标保持 17，无新增生产依赖。
- 桌面命令：`JAVA_HOME='/Applications/PyCharm.app/Contents/jbr/Contents/Home' bash scripts/verify-probe-logic.sh`；最终退出码 0，11 组 PASS。分组测试从 8 增至 39 检查，选择测试从 41 增至 60 检查；布局 344 检查、翻译试用 22 检查、审查回归 7 用例等其余套件通过。数量是断言数，不等于手机场景覆盖率。
- 协议/HTTP：重新编译并通过 JUnitCore 执行 `TranslationProtocolTest` 与 `DeepSeekProviderTest`，退出码 0，`OK (9 tests)`。类路径为 `.verification/bubble-full-types`、JSON、JUnit 4.13.2、Hamcrest；输出在 `.verification/bubble-protocol-tests`。测试使用内存模拟，未读取真实 Key 或调用 DeepSeek。
- 最终生产类型检查采用 `javac --release 17 -encoding UTF-8 -proc:none -Xlint:deprecation`、本地 Android/ML Kit 类型库和临时 R/BuildConfig，输出 `.verification/bubble-full-types`；全部 32 个生产 Java 编译退出码 0，仅原有 5 条 `AccessibilityNodeInfo.recycle()` 弃用警告。此检查不是完整 Android 构建，也不能验证触摸事件或窗口实际位置。
- 独立复审在修正后重新编译原复现：候选整组为空、请求消息数为 2，39 项分组测试通过；复查未发现新增重要缺陷。批量回调和拖动分支的结论来自代码审查，不是 Android 手势实测。
- Ruby 严格核对本轮工作区 8 份变更 Markdown：UTF-8 有效、无替换字符、相对链接目标存在；检查退出码 0。`git diff --check` 退出码 0。Windows 仍应按 `WINDOWS_ENCODING.md` 指定 UTF-8 打开，不能用本机检查宣称所有 Windows 编辑器默认编码都正确。
- 真机/Windows 待验证：完整 Gradle 编译、资源打包、lint、APK 身份/签名、G01～G04/B01～B05。当前 Mac 未找到完整 Android SDK 或可用设备；不使用版本 6 的 APK/手机结论替代。新增入口动作的运行时效果必须通过 B05 验证。

本轮交接仍以 [本轮优化验收与交接](本轮优化验收与交接.md) 为唯一执行入口；具体用例见 [DEVICE_TESTS](DEVICE_TESTS.md)。旧日志计数按历史保留。

## 2026-09-21 自动分组与附近显示

- 用户明确要求优先推进自动聊天分组与气泡附近中文显示。工作目录 `/Users/fanli0629/Documents/Codex/sakurazaka-upload.N1LNwy`，先快进获取最新文档 `92fc7cc`，再建立 `codex/chat-bubble-layout` 分支。本轮修改尚未提交或上传。
- 版本：`0.6.0-bubble-trial` / 7；生产依赖与权限不变，同步两个 Windows 验证脚本默认版本。
- 分组：保留原 collectionItem 依据，补充 CollectionInfo / RecyclerView / ListView 中有重复且不相交的直接子项；同来源、同窗口、边界有效才合并，辅助信息阻止跨越。保留重复位置、原文和来源。候选区可整组选中，并展开取消组内某行；单条候选和消息组保持原阅读顺序。未知结构仍需人工选择，不宣称已自动过滤所有人名。
- 显示：翻译成功后可点“贴近原文显示”，独立不可触摸窗口按实际屏幕坐标放置完整译文；避开已知文本/媒体/控件、系统/键盘区域与入口按钮。提供常规宽度和不小于 140dp 的侧栏测量选项，最多四条；放不下则保留完整卡片。点“返回译文”查看全部内容，切换不联网。
- 生命周期：当前页面身份与原单请求机制继续生效；滚动、切页、锁屏、旋转及新窗口 Insets/尺寸变化清理旧位置，根视图与 PageToken 防止迟到布局回调污染下一页。保留最近移除的自身窗口 ID 处理排队的移除事件，不忽略整个本应用包。
- 测试先行证据：新分组测试先在旧算法中因 `repeated direct children of RecyclerView identify separate rows` 断言失败；布局测试先因 `nearby rectangle is placed` 断言失败。实现后通过。审查追加窄栏用例，首次发现测试障碍物错误覆盖整条侧栏，修正为相邻原文实际宽度后通过。
- 基线：11 组测试增加前，9 组桌面自测退出码 0。本机旧临时 JDK 的 jvm.cfg 已不存在，改用已安装 PyCharm JBR `25.0.3`，Java 源码仍 `--release 17`，没有修改系统 Java 或生产依赖。
- 最终桌面命令：`JAVA_HOME='/Applications/PyCharm.app/Contents/jbr/Contents/Home' bash scripts/verify-probe-logic.sh`，退出码 0，11 组 PASS；新增 MessageGroupingSelfTest 8 项、BubbleLayoutSelfTest 344 项（含不同高度的边界/碰撞检查）。既有 CandidateSelectionSelfTest 41 项、TranslationTrialSelfTest 22 项、ReviewRegressionSelfTest 7 项和其余套件全部通过。
- 类型检查：`javac --release 17 -encoding UTF-8 -proc:none -Xlint:deprecation` 编译全部 32 个生产 Java（含 Service/OcrProcessor）至 `.verification/bubble-full-types`，退出码 0。使用既有 Android 16 API 类库及官方 Maven 下载的 ML Kit/GMS 类型依赖、Lifecycle/AndroidX/Kotlin 辅助类库；临时 R/BuildConfig 声明只供类型检查。初次因缺 Lifecycle、OptionalModuleApi、MlImage 等辅助类失败，补齐检查类路径后通过；最终只有原服务 5 处 recycle() 弃用警告，没有新增代码错误。**这不是 Gradle、资源编译、D8、lint 或 APK 构建。**
- 协议回归：Javac 编译两份 `app/src/test` 测试，JUnitCore 运行 TranslationProtocolTest 和 DeepSeekProviderTest，退出码 0，`OK (9 tests)`；使用本地模拟，不读取 Key 或请求 DeepSeek。产物在 `.verification/bubble-protocol-tests`。
- 语法检查：`java .verification/SourceSyntaxCheck.java .`，退出码 0；32 Java、6 XML 通过。`git diff --check` 通过。工具依赖、编译输出和临时声明均在忽略的 `.verification` 中。
- 独立代码审查提出窄栏宽度、键盘/窗口变化失效及候选阅读顺序问题，已处理；复查无剩余 Critical/Important 发现。审查不等于真机通过。
- 未执行：版本 7 的完整 Android Gradle 构建、lint、APK/签名产物检查、PowerShell 实际运行、真实 API、Android UI/无障碍回调和全部 G/B 真机用例。本机缺完整 SDK 和设备。版本 6 的真实测试结论不能移用。
- 下一步：按本轮交接在 Windows 构建版本 7，用两位偶像页面核对真实列表结构，重点验证键盘仅弹出/收起、附近层滑动穿透、窄屏/长文回退、整组后逐行取消及反复显示/返回。

## GitHub 分支交付准备（2026-09-17）

- 用户明确要求上传。目标为 origin/codex/node-candidates，master 保持原基线；下方“未提交/未推送”是当时的历史状态。
- 推送前查询远程 master 为 6214f15；重跑九组桌面自测和九项 JUnit 模拟测试，均通过。Android 构建、真实接口、APK 与真机仍未验证。
- 当前交接与 README 已说明获取分支的方法；交付身份以远端分支实际提交为准，推送完成须比较远端和本地提交，不能只凭本地 commit 成功宣称已上传。

## 2026-09-17：交接执行规则补齐

- 按用户要求，在当前交接第 0 节明确“先核对源码/环境、通过附证据、矛盾先记录核实”，不依赖模型名称或历史结论。
- 新增第 7～10 节：Windows 接手只读核验、生产/测试文件定位表、环境/代码/API/真机故障分流、矛盾记录模板、分阶段证据标准、最终交付格式及可复制接手指令。
- 重点说明 HEAD 相同但工作区未提交时源码不一定相同；源码 Java 17 目标与 Gradle 运行 JDK 不混淆；NO-SOURCE/SKIPPED 不算通过，缓存结果须说明来源。
- 本次仅修改交接相关文档，不修改应用代码，也不新增 Android/API/真机通过结论。接手入口与实施计划已指向同一份规则，避免另建重复状态文档。

## 2026-09-17：中文翻译试用版（最新）

- 用户确认先写代码再回机验证；保留前轮所有修改，版本升为 0.5.0-translation-trial / 6；本轮尚未提交/推送。
- 已接入 MessageGrouper、SymbolProtector、TranslationProtocol、DeepSeekProvider、TranslationRunner、TranslationSettings、TranslationSettingsActivity 和 TranslationPanel；TranslationResult 改为带 ID 的 Item，并统一校验/重排。
- 实现：同列表项、同来源且不重叠的选中节点才建议合并；已有符号占位保护；原文确认后显式发送；中文对照卡片；两份可编辑风格；Key 加密；设置页隔离；超时取消且无自动重试。不是 OCR 已丢表情恢复，不含图片上传或相邻气泡。
- 新测试起初因模块/API 缺失编译失败，实现后通过。同列表项不同字段来源误合并用例在原实现中运行失败，增加同来源条件后通过。
- 最终桌面验证：九组自测 PASS；TranslationTrialSelfTest 22 项、CandidateSelectionSelfTest 41 项、ReviewRegressionSelfTest 7 项，另有单任务/取消/迟到响应/再启动测试。
- 直接编译并执行 JUnit：TranslationProtocolTest 5 项 + DeepSeekProviderTest 4 项 = 9 项 PASS。使用本地模拟连接，不访问 DeepSeek，不含真实 Key。覆盖 JSON 字段类型、占位符、错 ID、截断、HTTP 重定向/限流、取消、响应上限。
- 本地补充类型检查：从 Maven Central 下载 Android 16 类库和 ECJ，按 Java 17 源码目标检查除 Service/OcrProcessor 外的 28 个生产 Java 文件，零错误/警告。初次 JDK 17 直接读 Java 21 类库失败、随后类库中的 java.* 与宿主模块冲突；仅调整验证工具与类库范围，未更改工程运行依赖。使用临时 R 声明，不是 Android 资源编译或完整 Gradle 构建。
- 全部 30 个生产 Java 和 6 个 XML 的语法检查通过。临时依赖和辅助文件均在 .verification（忽略目录），不提交。
- 生产运行依赖保持原 ML Kit，仅新增 JUnit/org.json 测试依赖。Windows 总入口新增 testDebugUnitTest；产物检查现在要求 INTERNET 存在、ACCESS_NETWORK_STATE 不存在；Manifest 禁明文网络，共享偏好显式排除备份。
- 未执行：真实 DeepSeek 调用、Android Gradle 构建/lint/APK、PowerShell 执行、Keystore/窗口/手机行为。官方文档站本轮 DNS 失败，默认模型及真实协议能力需原机联调核实。
- 交接已同步当前入口、状态、设计、审核、长期计划和旧入口提示；详细验收 C01～C13 见 [本轮优化验收与交接](本轮优化验收与交接.md)。

## 2026-09-17：原文保真与统一候选

- 基线 `6214f15`，分支 `codex/node-candidates`；源码版本 `0.4.0-probe-candidates` / 5，本轮修改尚未推送。
- 已实现 CandidateSelection、CandidatePanel、PageToken，并更新请求工厂、来源/边界/风险传递；替换旧 OCR 专属选择类及其测试，不另建第二套生产选择流程。
- 节点先可选，OCR 追加保持勾选；辅助信息默认不选且折叠；截图失败保留节点；截图、原文和诊断可折叠；标题可核对版本。
- 回调携带原页面身份，旧卡片操作不能关闭新卡片；OCR 完成在主线程更新 UI，物理任务完成和预览解绑共同决定图片回收。自身窗口事件使用已登记窗口 ID，真机需验证是否存在误清理。
- 新测试先因新增类型/方法尚未实现而编译失败，实现后通过。七组桌面自测全部通过：CandidateSelectionSelfTest 41 项，ReviewRegressionSelfTest 7 项；包含组合 Emoji/换行原样传递、来源冲突原子拒绝、旧页面拒绝、来源与警告保留等。
- JDK 语法解析已检查 22 个生产 Java 和 6 个 XML 文件；仅解析语法，不做 Android 类型解析、资源编译或 lint。
- 当前没有 Android SDK/完整缓存，未执行新版 Gradle 构建、APK 权限/签名检查、Windows PowerShell 执行或真机测试。历史 0.3.1 的 PASS 不代表新版通过。
- 当前验收入口：[本轮优化验收与交接](本轮优化验收与交接.md)。最终检查：`git diff --check`、修改/新增文本的 UTF-8 解码和替换字符检查、已改 Markdown 相对链接、两份测试清单的源文件/类名存在性均通过；未新增聊天截图、密钥或构建产物。

## 2026-09-16 历史执行记录

范围：落实已确认的性能、可读性、结构和安全优化。当前没有新增联网功能。

## 执行计划

- [x] 协调器改名为 captureCoordinator；接入 TranslationRequestFactory。
- [x] 用回归测试复现节点排序、OCR 选择状态和状态机问题。
- [x] 统一候选顺序和选择状态，清理关闭时残留的数据。
- [x] 抽出 NodeTreeReader 和 OcrProcessor，删除未调用的旧拼接路径。
- [x] 整理测试入口，避免测试类打入 APK。
- [x] 更新审核报告、当前状态和真机验收清单，记录实际验证范围。

## 验证证据

- ReviewRegressionSelfTest 首次运行：七项失败（节点 ID 排序、同坐标候选顺序、空白候选、已完成任务重启、跳过截图、空白译文、重复消息 ID）。对应修复后七项通过。
- OcrSelectionSelfTest：新模块建立前编译失败；实现后验证反向勾选、原文/换行、请求顺序、取消选择、未知 ID、清空和重新取字。
- `scripts/verify-probe-logic.ps1` 已在 Windows PowerShell 5.1 运行：七组测试通过，Java 17 / UTF-8 / -Xlint:all，无编译警告输出。
- 新增 `scripts/verify-android-artifact.ps1` 和 `scripts/verify-pre-device.ps1`，并在 Windows PowerShell 5.1 端到端通过。Android Java 编译、lint 和 assembleDebug 成功；APK 静态检查确认版本、v2 签名、无网络权限以及日文 OCR native/model 均符合要求。
- 本轮 APK：`0.3.1-probe-review`/4，`53,400,368` 字节，SHA-256 `463CE6B4EAD735B07EF442B5DE09B2BF551A614517F18FA7AA95D9DA22C6E249`。用户已提供首轮目标页面视觉证据；ADB 当前无设备，设备端版本身份和完整真机矩阵仍待确认。
- 更新文档已检查 UTF-8 解码、相对链接、测试清单完整性和 XML 语法，检查通过；这不是 Android 资源编译结果。
- 独立代码复核未发现明确新增的严重/重要缺陷；排序一致性、选择清理和任务阶段调用与测试相符。Android 构建门槛现已通过，但 Android 回调、UI 与资源生命周期结论仍需真机验证。
- 本轮开始前源码备份在 /private/tmp/sakurazaka-before-review.SnDNhd/source-before.tar.gz（临时本机备份，跨电脑应以原工程 Git 检查点为回退依据）。

## 查验中发现的说明偏差

1. 节点 ID 是 n2:text / n10:text，原来的尾部数字解析没有覆盖这个格式。
2. ML Kit 返回顺序不等于屏幕坐标顺序；现有 OCR 汇总只保持了返回顺序。
3. OCR 全部标为 MEDIA_CANDIDATE，工厂排除 METADATA 不代表 OCR 的作者/时间已自动过滤。
4. 一行 OCR 是一个片段，不一定是一条聊天消息；确认数量应显示为片段数。
5. Java 自测不编译 Android Service，也不等价于 APK 构建或真机通过。

## 2026-09-19 Windows 接手核验与构建

- 工程目录：`D:\Projects\sakurazaka-translator`；远程为 `https://github.com/fanli20040629-max/sakurazaka-translator.git`。
- 构建时源码身份：分支 `codex/node-candidates`，当时 HEAD/origin 均为 `dfbcded1bfd70b834393d905e9047dd5c1eee459`；远程交付分支当时未发现该提交之后的提交。该提交是本轮 APK 与真机结果对应的源码身份，不因后续文档提交而改变。
- 接手前工作区：无已跟踪修改；保留未跟踪目录 `0914审核结果/0914审核结果/`，未覆盖或清理。
- 环境：JDK `25.0.3`（`D:\android_stufio\android-studio\jbr`）；Android SDK `D:\Android\Sdk`，`android-36` 与 Build Tools `36.0.0` 存在；ADB 37.0.1。接手首次检查时无设备连接，随后用户接入 SM-S9060 并继续真机测试。
- 实际命令：`scripts/verify-pre-device.ps1 -JavaHome ... -SdkRoot ... -ExpectedVersionCode 6 -ExpectedVersionName '0.5.0-translation-trial'`；使用项目内 `.gradle-home`，退出码 0。
- 结果：9 组桌面逻辑测试 PASS；`compileDebugJavaWithJavac`、`testDebugUnitTest`、`lintDebug`、`assembleDebug` 成功；lint 报告位于 `app/build/reports/lint-results-debug.html`。
- APK：`app/build/outputs/apk/debug/app-debug.apk`，包名 `com.fanli.sakurazakatranslator`，版本 `0.5.0-translation-trial (6)`，大小 `53461586` 字节，SHA-256 `7B32101BC4B19A5C1111B602395F4CDF71D4EE5485035F070C94DD3C1ADE4778`，v2 签名通过；包含 `INTERNET`，不含 `ACCESS_NETWORK_STATE`，日文 OCR native pipeline/model 存在。
- 阶段结论：代码同步和构建验证通过。该结论记录的是连接手机之前的检查点；当晚随后完成了安装身份核对、主流程真机测试和真实 DeepSeek 联调，最终结果如下。

## 2026-09-19 晚间真机执行与收尾

### 设备、安装和服务

- 设备：Samsung SM-S9060，Android 16；为保护隐私，文档不记录设备序列号。
- 用户自行安装现有 debug APK；ADB 确认设备端包名 `com.fanli.sakurazakatranslator`、版本 `0.5.0-translation-trial (6)`，安装更新时间 `2026-09-19 22:29:27`。未自动卸载应用、未清除数据、未替换本机签名。
- 无障碍服务曾出现“设置显示开启、系统实际列入崩溃服务”的状态。用户关闭再开启后恢复绑定，“取字探针”出现；后续检查结束时 `Crashed services:{}`。这次恢复不涉及源码修改。

### 主流程验证

- 合成页取字成功，系统日志出现 `OCR process succeeded via visionkit pipeline`；候选卡可显示和滚动，候选不会在确认前自动联网。
- 缺少 Key 时发送被本地阻止并显示提示，原文保留，未观察到自动重试或崩溃。
- 用户仅在手机设置页输入有效 Key；USB 观察到本应用访问 `api.deepseek.com:443`，用户确认 DeepSeek 侧产生费用。随后抓屏看到编号对应的中文结果，`💗`、`👩🏽‍💻` 等可见符号保留。真实中文翻译主链路通过，但应用日志没有记录完整 HTTP 状态码、额度和服务端错误矩阵。
- 多条真实消息一次得到 20 个片段，采集耗时 1210 ms；正文可进入候选并翻译。作者名、日期时间、按钮和 OCR 结果仍会作为候选出现，当前需要人工只勾选正文。观察到的截图中作者/时间未进入最终确认正文，但“自动识别并排除作者/时间”尚未实现。
- 图文混排样本生成 3 条编号对应译文，聊天气泡文字和可见花朵/爱心正常；图片内部文字未获得清晰结果。用户确认该功能使用频率低，本轮不修复，记录为已知限制。
- 长文本能生成中文卡片、连续段落可见并可滚动，未崩溃；没有逐段完成全文对照，因此不宣称零截断、零漏段。

### 生命周期、安全和稳定性

- 设置页焦点确认为 `TranslationSettingsActivity`；Key 输入为密码框，已保存值不回显，页面未出现取字浮窗或旧译文卡片。
- 删除 Key 后发送被阻止，提示用户先设置 Key，近期日志未发现 `api.deepseek.com` 请求；重新输入 Key 后再次得到中文译文。留空保存、重启后解密和设备迁移未全部单独验证。
- 锁屏后浮窗被清除，解锁后旧确认和旧译文未恢复，应用未崩溃。本次设备解锁后没有自动恢复助手无障碍服务，用户手动重开后服务正常绑定；暂记为系统/服务生命周期现象，不能宣称自动恢复通过。
- ADB 连续执行 10 次打开/返回助手，无应用崩溃，最终崩溃服务列表为空。该循环没有重复发起付费翻译，也未覆盖旋转、字体调整、约 20 次完整取字/翻译和内存趋势。
- USB 启动目标包 `jp.co.sonymusic.communication.sakurazaka` 后，前台焦点正确进入目标应用；返回助手后焦点恢复。未观察到新的错误卡片或崩溃。范围过滤只完成本次样本复核，未遍历所有非允许 App。

### 问题处置与改动边界

- 当晚没有发现需要立即修改源码才能继续主流程的阻塞缺陷，因此没有实施代码修复，也没有变更版本号或重新生成不同身份的 APK。
- 人名/时间候选、节点与 OCR 自动配对、结果贴近原气泡、图片 OCR、完整取消/网络错误矩阵属于未实现或未充分验证项，不能写成已完成。
- 本轮更新详细记录 `docs/优化执行记录.md`、`docs/DEVICE_TESTS.md`、`docs/本轮优化验收与交接.md`，并同步 `README.md`、`project_state.md`、`docs/STATUS.md` 的当前摘要；历史段落不回填为新版通过。设备截图/XML 保留为本地未跟踪证据；原电脑未跟踪目录 `0914审核结果/0914审核结果/` 保持不动。
- 真机记录已提交为 `f408d3e9f3bca79dd0a26282d507605167a489b0` 并推送；远程 `master` 随后由 `6214f15` 快进到该提交，`master` 与 `codex/node-candidates` 当时完全一致。没有强制推送，也没有删除交付分支。
- 设备截图、UI XML 和原审核目录均未进入提交或 GitHub；本机使用 `.git/info/exclude` 排除这些路径，降低以后执行批量暂存时误上传私人证据的风险。
