# 櫻坂翻译助手

> 当前（2026-09-17）：`0.5.0-translation-trial` / 6，已写入保守节点分组、已有符号保护、DeepSeek 文本翻译、两份风格设置和中文结果卡片。九组桌面逻辑测试通过；真实 API、Android 构建和手机验证待执行。以 [本轮优化验收与交接](docs/本轮优化验收与交接.md) 为入口；下面 0.4 及更早的状态仅为历史。当前新增 INTERNET 权限用于显式文本发送，不上传截图。本轮交付分支为 `codex/node-candidates`；请获取该分支，`master` 不包含本轮修改。

> 2026-09-17 当前交接入口：[本轮优化验收与交接](docs/本轮优化验收与交接.md)。源码版本 `0.4.0-probe-candidates` / 5：节点可先勾选，OCR 后续追加不重置选择，原文、来源、风险与页面身份随本地请求保留。七组桌面逻辑测试通过；本机无 Android SDK，新版 Android 构建、APK 和真机验收仍待 Windows 完成。本轮修改尚未推送 GitHub；以下旧日期的构建与截图只证明旧版本。

当前模块职责和剩余门槛见[当前代码审核与改进方案](docs/当前代码审核与改进方案.md)。OCR 仍需手动排除作者/时间；选中的行称为“片段”，不是已经正确分组的聊天消息。

2026-09-16 首轮真机结果及 Emoji 错误的后续方案见[真机测试结果与后续修复建议](docs/2026-09-16真机测试结果与后续修复建议.md)。

当前版本是 P0.5 取字探针，目标是先验证 Android 16 真机上目标应用的无障碍节点、指定窗口截图和日文 OCR 能力。

## 当前状态

- 2026-09-14：已依据首批新版目标页真机截图完成 M2/M3 第一批：节点结果分区、默认隐藏逐条坐标、OCR 复选候选和选择汇总。`0.3.0-probe-select` 已通过逻辑测试、lint、构建和 ADB 覆盖安装，等待目标页交互复测；详细入口为[正文整理版开发方案 2.0](docs/PROBE_TEXT_OPTIMIZATION_HANDOFF.md)。
- P0：工程、Wrapper 和命令行 `assembleDebug` 已验证。
- P0.5：旧探针已有安装、稳定卡片、合成及部分目标截图/OCR 记录；生命周期、正文结构和完整四类页面验收仍有缺口。旧逻辑自测 PASS 不代表这些行为全部通过。
- DeepSeek、API Key、翻译卡片和目标应用适配尚未接入。

## 构建

在项目根目录使用：

```powershell
& .\scripts\verify-pre-device.ps1
```

该入口依次运行逻辑测试、Android 编译、lint、APK 构建和产物检查。APK 生成后，以终端输出的绝对路径为准。不要提交 `local.properties`、API Key、截图、聊天正文或构建产物。

## 真机探针步骤

1. 安装 Debug APK，打开应用并先勾选“启用本页合成测试”。
2. 进入 Android 无障碍设置，开启“櫻坂翻译助手”。
3. 返回内置合成页，点击“取字探针”，先验证重复文本、长文和图片文字。
4. 记录节点文本、OCR 文本、页面类型和错误码；合成页通过后，再根据设备前台证据确认并保存目标 App 包名。

截图成功只表示截图接口成功，需核对 OCR 文本后才能判定正文取字通过。

## 已知限制

- 目标包名已有前台证据：`jp.co.sonymusic.communication.sakurazaka`；目标实际版本及当前连接状态在下次手机测试时核对。
- 当前探针只在本应用合成页或用户保存的目标包名处显示按钮。
- 已观察的目标节点能保留部分样本的符号和换行；长文、重复、截断、系统性页面/锁屏回归尚未完成，不能推广为所有页面可用。
